# App Icon Source

`logo-placeholder.svg` is the temporary source icon for dailytrack branding.

When you have the final logo, replace this file and regenerate platform icons:

```bash
npm run tauri icon src-tauri/icons/logo-placeholder.svg
```

This updates the bundled `.icns`, `.ico`, and PNG variants used by Tauri packaging.

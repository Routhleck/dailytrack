# App Icon Source

`dailytrack.png` is the current source icon for dailytrack branding.

When you update the logo, replace this file and regenerate platform icons:

```bash
npm run tauri icon src-tauri/icons/dailytrack.png
```

This updates the bundled `.icns`, `.ico`, and PNG variants used by Tauri packaging.

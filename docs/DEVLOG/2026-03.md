# DEVLOG 2026-03

## 2026-03-18
- Date: 2026-03-18
- Goal: Complete Phase 1 documentation and collaboration baseline before implementation.
- Changes:
  - Added root `AGENTS.md` with mission, guardrails, workflow, DoD, and logging policy.
  - Added `CHANGELOG.md` initialization.
  - Added architecture, data model, parser, storage, setup, QA, and release docs under `docs/`.
  - Added ADR-0001 (Tauri stack) and ADR-0002 (normalized markdown save strategy).
- Validation:
  - Confirmed all planned documentation files exist and are non-empty.
  - Cross-checked source-of-truth rule consistency across AGENTS, data model, and parser/storage specs.
- Risks:
  - Setup commands in `DEV_SETUP.md` remain provisional until scaffolding is generated.
- Next:
  - Start Phase 2: scaffold Tauri + React + TypeScript + Tailwind app shell and routes.

## 2026-03-18 (Phase 2)
- Date: 2026-03-18
- Goal: Scaffold executable desktop app shell with local data root bootstrap.
- Changes:
  - Initialized Vite React TypeScript project and Tauri runtime (`src-tauri/`).
  - Added Tailwind CSS and app-level layout shell with sidebar and route placeholders.
  - Added Tauri commands for local file operations and `ensure_data_root` bootstrap.
  - Added settings data root context and persistence in localStorage.
- Validation:
  - `npm run build` passed.
  - `npm run tauri -- info` passed environment checks.
- Risks:
  - App shell was functional but feature pages still placeholders.
- Next:
  - Implement daily/weekly/body feature modules with parser and serialization logic.

## 2026-03-18 (Phase 3-7)
- Date: 2026-03-18
- Goal: Deliver functional MVP flows for daily/weekly/body/dashboard and test baseline.
- Changes:
  - Implemented daily markdown parser/serializer/service and Daily page structured/raw editor.
  - Implemented weekly markdown parser/serializer/service and Weekly page structured/raw editor.
  - Implemented body CSV parser/service and Body page table/form/charts.
  - Implemented Dashboard summaries and Daily/Weekly list views.
  - Added Vitest test suite for daily, weekly, and body CSV roundtrip behavior.
  - Updated README and changelog to reflect shipped MVP behavior.
- Validation:
  - `npm run test` passed (3 files, 6 tests).
  - `npm run build` passed.
- Risks:
  - Production bundle size warning due chart dependency footprint.
  - Structured save normalizes markdown and may not preserve arbitrary custom formatting.
- Next:
  - Optional follow-up: split chart bundle with lazy loading and improve formatting preservation strategy if needed.

## 2026-03-19
- Date: 2026-03-19
- Goal: Reduce initial frontend bundle size by splitting heavy route modules.
- Changes:
  - Refactored `src/app/router.tsx` to use `React.lazy` and `Suspense` for all top-level pages.
  - Ensured Body page and `recharts` are loaded on demand rather than in the startup bundle.
- Validation:
  - `npm run build` passed and emitted split chunks.
  - `npm run test` passed (3 files, 6 tests).
- Risks:
  - Route-level loading fallback is minimal and could be improved visually later.
- Next:
  - Continue with next functional iteration (format-preservation improvements or UX polish).

## 2026-03-19 (Export/Import)
- Date: 2026-03-19
- Goal: Add local data export/import so tracker files can move across different computers.
- Changes:
  - Added backend commands `export_data_bundle` and `import_data_bundle` in `src-tauri/src/lib.rs`.
  - Added recursive copy utility and import source validation (`daily/`, `weekly/`, `body.csv`).
  - Added export safety check to prevent exporting into the current data root subtree.
  - Added Windows-friendly fallback (`USERPROFILE`) for default data root resolution.
  - Extended Settings page with Export Data and Import Data forms.
  - Added frontend FS API wrappers `exportDataBundle` and `importDataBundle`.
  - Updated README and filesystem docs with portability workflow.
- Validation:
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Import is file-level copy/merge and does not do semantic conflict resolution.
- Next:
  - Optional: add built-in ZIP compression and OS-native file picker dialogs.

## 2026-03-19 (Profiles and Preferences)
- Date: 2026-03-19
- Goal: Support multi-profile workflows, template presets, and per-profile tracking preferences.
- Changes:
  - Added profile backend commands in `src-tauri/src/lib.rs`:
    - `list_profiles`
    - `ensure_profile`
    - `create_profile`
    - `delete_profile`
  - Migrated runtime model to `baseRoot/profiles/<profile>/...` and added legacy-to-default profile bootstrap copy.
  - Extended `DataRootContext` to manage:
    - base root
    - active profile
    - profile list
    - profile CRUD/switch actions
  - Added `Profiles` page:
    - create profile
    - switch profile
    - delete non-active profile
    - choose/edit template preset at creation
    - edit active profile templates
  - Added `Preferences` page + context and per-profile `preferences.json`.
  - Applied preferences to UI behavior:
    - Daily optional section toggle
    - Weekly section visibility toggle
    - Body metric visibility toggle (weight/waist/note)
  - Updated README/docs for profile-based storage and preference model.
  - Updated eslint ignore to exclude `src-tauri/target/**` generated artifacts.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Preference toggles affect structured UI visibility, but hidden data remains in files.
  - Import still performs file-level merge/overwrite without semantic conflict handling.
- Next:
  - Add OS-native folder picker for export/import/profile root selection.

## 2026-03-19 (Realtime Sync Pass)
- Date: 2026-03-19
- Goal: Move core pages to near real-time behavior for local edits and external file changes.
- Changes:
  - Added `src/lib/liveSync.ts` event bus for cross-page refresh notifications.
  - Emitted change events from profile/settings/preference operations.
  - Daily/Weekly pages:
    - Added debounced autosave (structured/raw modes).
    - Kept manual `Save now` fallback.
    - Added external file polling with dirty-state guard to avoid overwriting unsaved local edits.
  - Body page:
    - Emits change events after save.
    - Added external file polling with non-reentrant sync refs.
  - Dashboard and daily/weekly list pages:
    - Reload on live events.
    - Added periodic polling/focus refresh fallback.
  - Settings import now emits global refresh event after successful import.
  - Updated README/FS storage/AGENTS docs to reflect profile layout and realtime behavior.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - External change detection currently uses polling, not native OS file watchers.
  - Concurrent writes are mitigated via dirty-guard logic but not protected by file locking.
- Next:
  - Optional: replace polling with native watcher bridge in Tauri for lower latency and fewer redundant reads.

## 2026-03-19 (Default Path Naming Alignment)
- Date: 2026-03-19
- Goal: Align default local data folder naming with project branding while preserving existing users.
- Changes:
  - Updated backend default data root name from `life-tracker-data` to `dailytrack-data`.
  - Added backward-compatible fallback: if legacy `life-tracker-data` exists and new path does not, use legacy path automatically.
  - Updated Settings placeholder and storage docs (`README`, `AGENTS`, `FS_STORAGE`, `DATA_MODEL`, `DEV_SETUP`).
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Historical log entries still mention the legacy path by design.
- Next:
  - Optional: add one-click “migrate base root” action in Settings (copy old root to new root and switch).

## 2026-03-19 (One-Click Data-Root Migration)
- Date: 2026-03-19
- Goal: Add a safe one-click migration flow to copy existing data root and switch app root.
- Changes:
  - Added Tauri command `migrate_data_root(source_root, destination_root, overwrite)`:
    - validates source exists
    - creates destination root
    - blocks same-path and nested-path migrations
    - copies files recursively with optional overwrite
    - ensures destination profile layout is valid
  - Added frontend API wrapper `migrateDataRoot`.
  - Extended `DataRootContext` with `migrateDataRoot(...)` action.
  - Added `Settings -> Migrate Data Root` form:
    - destination path input
    - optional overwrite conflicts toggle
    - migrate-and-switch action
  - Clarified in UI/docs that `Save Data Root` is a switch-only action.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Migration currently performs copy+switch (non-destructive) and does not delete source automatically.
- Next:
  - Optional: add post-migration verification summary (file counts/profile counts) in UI.

## 2026-03-19 (Migration UX Safety Polish)
- Date: 2026-03-19
- Goal: Reduce migration misconfiguration risk and make switch-vs-migrate intent clearer.
- Changes:
  - Added migration pre-validation in Settings UI:
    - reject same-path source/destination
    - reject nested source/destination
  - Added quick action button in Data Root section:
    - `Use This Path for Migration`
  - Updated QA checklist and README safety notes for migration behavior.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - UI validation uses normalized string checks and defers final path correctness to backend canonical checks.
- Next:
  - Optional: add post-migration integrity summary in UI (copied profile/file counts).

## 2026-03-19 (GitHub Release CI)
- Date: 2026-03-19
- Goal: Prepare public-repo release automation for Windows and macOS builds.
- Changes:
  - Added `.github/workflows/publish.yml`:
    - triggers on `v*` tags and manual dispatch
    - matrix builds for:
      - `windows-latest`
      - `macos-latest` (`aarch64-apple-darwin`)
      - `macos-latest` (`x86_64-apple-darwin`)
    - runs frontend checks (`lint`, `test`, `build`) before bundling
    - uses `tauri-apps/tauri-action@v0` to upload artifacts to a draft GitHub Release
  - Updated README with release CI usage and tag-based publish commands.
  - Updated release checklist with CI/release-asset verification items.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - Workflow syntax reviewed locally.
- Risks:
  - Current pipeline builds unsigned binaries/installers.
  - Signing/notarization secrets are not configured yet.
- Next:
  - Optional: add code-signing/notarization integration once certs and secrets are available.

## 2026-03-19 (First-Launch Template Setup + Preset Expansion)
- Date: 2026-03-19
- Goal: Improve first-run onboarding and offer richer template choices with bilingual content options.
- Changes:
  - Changed backend `ensure_data_root` return payload from root string to:
    - `root`
    - `isFirstRun`
  - Added first-run empty-root detection in backend bootstrap path.
  - Extended frontend `DataRootContext` with:
    - `needsInitialTemplateSetup`
    - `completeInitialTemplateSetup(...)`
    - pending first-launch setup persistence via localStorage guard
  - Added blocking `InitialTemplateSetupModal` shown only for brand-new empty root initialization.
  - Reworked template preset model to bilingual variants and expanded preset set:
    - Balanced
    - Minimal
    - Fitness
    - Deep Work
    - Creator
    - Student
    - Recovery
    - Social
    - Blank Skeleton
  - Updated `Profiles` page to support preset language selection (`en`/`zh`) while preserving editable template textareas.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - First-run detection is based on root-directory emptiness before bootstrap and may be affected by unrelated pre-existing files in custom root path.
- Next:
  - Add optional in-app “rerun template setup wizard” action for current active profile.

## 2026-03-19 (Bilingual UI Layer)
- Date: 2026-03-19
- Goal: Add practical EN/ZH UI switching across core pages without changing markdown schema contracts.
- Changes:
  - Added lightweight i18n infrastructure:
    - `I18nProvider`
    - local storage persistence (`dailytrack.uiLanguage`)
    - typed message dictionary (`en`/`zh`)
  - Added sidebar language toggle (`EN` / `中文`) and default locale detection from system language.
  - Localized core UX surfaces:
    - sidebar navigation
    - app shell status line
    - dashboard
    - daily/weekly list pages
    - daily/weekly note detail pages
    - body page
    - profiles
    - preferences
    - settings
    - first-launch template modal
  - Maintained parser compatibility by keeping template markdown headings canonical in English.
  - Updated README/FS storage/QA checklist/changelog for onboarding and bilingual behavior.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Current localization coverage focuses on core page copy; rare backend-propagated raw error strings are not translated.
- Next:
  - Optional: split message dictionaries by feature module to reduce initial bundle footprint and improve maintainability.

## 2026-03-19 (Reset to First-Run State)
- Date: 2026-03-19
- Goal: Add a complete reset path so users can return to initial setup state from Settings.
- Changes:
  - Added new Tauri command `reset_tracker_data(data_root)`:
    - removes app-managed tracker paths under the selected base root:
      - `profiles/`
      - legacy `daily/`, `weekly/`, `templates/`, `body.csv`
    - preserves unrelated unknown files in the same root for safety.
  - Added frontend API wrapper `resetTrackerData`.
  - Extended `DataRootContext` with `resetTrackerData()` action:
    - executes backend reset
    - clears tutorial state flags
    - marks initial template setup pending for current root
    - bootstraps and emits global data change event
  - Added `Settings` danger-zone reset UI:
    - localized warning copy
    - requires exact `RESET` confirmation text
    - success path returns app to first-launch template setup modal
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Reset scope is intentionally app-path based; if users store tracker data outside expected folders, those files are not removed.
- Next:
  - Optional: add pre-reset local backup shortcut (export) inline in danger zone.

## 2026-03-19 (First-Use Guided Tutorial)
- Date: 2026-03-19
- Goal: Add a lightweight first-use in-app tutorial with replay support.
- Changes:
  - Added tutorial state storage:
    - `dailytrack.tour.pending.v1`
    - `dailytrack.tour.completed.v1`
    - `dailytrack.tour.dismissed.session.v1`
  - Hooked first-launch template completion to set tutorial pending flag.
  - Added sidebar-targeted guided overlay (`TutorialGuide`) with 5 steps:
    - Today
    - This Week
    - Body Progress
    - Profiles
    - Settings
  - Auto tutorial behavior:
    - starts once after first template setup
    - skip closes and clears pending auto prompt
    - finish marks tutorial completed
  - Added manual replay entry in `Settings` via `Start Tutorial`.
  - Added EN/ZH localized tutorial copy.
  - Updated README/QA/FS storage/Data model/changelog docs for tutorial workflow.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - Tutorial step anchors currently depend on sidebar structure and `data-tour` attributes.
- Next:
  - Optional: add page-level targeted steps and richer pointer guidance for specific controls.

## 2026-03-19 (Pure Autosave Notes)
- Date: 2026-03-19
- Goal: Switch daily/weekly note pages to pure autosave mode.
- Changes:
  - Removed manual save action from Daily and Weekly note toolbars.
  - Kept debounced autosave behavior (structured/raw modes) and save-state messaging.
  - Added passive status indicator (`Auto-save on` / `Saving...`) in note toolbar.
  - Updated docs and QA checklist to reflect no-manual-save behavior.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - If the app is force-closed during debounce window, very recent keystrokes may not be flushed yet.
- Next:
  - Optional: add immediate flush on app close/window blur to reduce debounce-window risk.

## 2026-03-19 (Open-Source README Refresh)
- Date: 2026-03-19
- Goal: Improve first impression for public repository launch.
- Changes:
  - Reworked root `README.md` into open-source friendly structure:
    - screenshots
    - value proposition
    - quick start
    - core features
    - storage model
    - onboarding/save behavior
    - CI release usage
  - Added screenshot placeholder assets:
    - `docs/assets/screenshots/dashboard.png`
    - `docs/assets/screenshots/daily-note.png`
    - `docs/assets/screenshots/weekly-note.png`
    - `docs/assets/screenshots/settings.png`
  - Added screenshot replacement guidance:
    - `docs/assets/screenshots/README.md`
  - Updated changelog for README/asset refresh.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - Placeholder screenshots are not real UI captures and should be replaced before broad announcement.
- Next:
  - Capture real app screenshots (EN first, optional ZH set) and replace PNG placeholders.

## 2026-03-19 (Branding Placeholder Pass)
- Date: 2026-03-19
- Goal: Add placeholder branding assets for app icon and improve README screenshot compatibility.
- Changes:
  - Added placeholder logo asset:
    - `src/assets/dailytrack-logo-placeholder.svg`
  - Wired placeholder logo into sidebar brand area.
  - Updated web favicon to placeholder logo:
    - `public/favicon.svg`
  - Added Tauri icon source placeholder and guidance:
    - `src-tauri/icons/logo-placeholder.svg`
    - `src-tauri/icons/README.md`
  - Regenerated desktop icon assets via:
    - `npm run tauri icon src-tauri/icons/logo-placeholder.svg`
  - Converted README screenshot placeholders to PNG and updated references.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - Placeholder logo is temporary and should be replaced with final brand asset later.
- Next:
  - Replace placeholder logo source and rerun `npm run tauri icon ...` before first public release tag.

## 2026-03-19 (Apply User Logo)
- Date: 2026-03-19
- Goal: Replace placeholder branding with provided `dailytrack.png`.
- Changes:
  - Added real logo assets:
    - `src/assets/dailytrack-logo.png` (optimized 128x128 for UI use)
    - `public/favicon.png`
    - `src-tauri/icons/dailytrack.png` (icon generation source)
  - Updated sidebar logo import to use PNG logo.
  - Updated `index.html` favicon link to PNG.
  - Regenerated Tauri desktop icons with:
    - `npm run tauri icon src-tauri/icons/dailytrack.png`
  - Removed legacy placeholder logo assets:
    - `src/assets/dailytrack-logo-placeholder.svg`
    - `src-tauri/icons/logo-placeholder.svg`
    - `public/favicon.svg`
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - If logo is updated again, Tauri icon regeneration should be rerun to keep bundle icons consistent.
- Next:
  - Optional: add separate monochrome icon variant for small-size display contexts.

## 2026-03-19 (Settings Simplification + Fixed Sidebar)
- Date: 2026-03-19
- Goal: Simplify data-root UX to migration-first and keep left navigation fixed while browsing long pages.
- Changes:
  - Removed direct `Save Data Root` form from Settings UI.
  - Kept migration flow as the single root-change path in UI.
  - Added visible hint in Settings that root change is migration-only.
  - Updated sidebar layout to sticky full-height panel:
    - left navigation remains fixed while right content scrolls.
  - Bundled user-provided README screenshots updates in same changeset.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - Direct root switching is still available at API/context layer but intentionally hidden from UI.
- Next:
  - Optional: fully remove unused root-switch APIs if no future scenario requires direct switching.

## 2026-03-19 (Release Signing Hardening)
- Date: 2026-03-19
- Goal: Prevent macOS Gatekeeper "app is damaged" issues in GitHub release builds.
- Changes:
  - Reverted local sidebar-scroll fix commit per product confirmation (behavior already acceptable in release build).
  - Updated `.github/workflows/publish.yml` for macOS jobs:
    - validates required Apple signing/notarization secrets
    - imports `.p12` certificate into temporary keychain
    - passes notarization/signing env vars into `tauri-action`
  - Updated README and release checklist with required secrets list.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - If any required Apple secret is missing/invalid, macOS publish jobs will fail fast by design.
- Next:
  - Configure secrets in GitHub repo settings and run next tag release to verify signed/notarized artifacts end-to-end.

## 2026-03-19 (Unsigned macOS Guidance in README)
- Date: 2026-03-19
- Goal: Reduce confusion for users installing unsigned macOS builds.
- Changes:
  - Added explicit Gatekeeper warning note in README for unsigned builds.
  - Added temporary quarantine-removal commands (`xattr -dr ...`) for testers.
  - Added practical lower-cost distribution guidance before Apple Developer subscription:
    - source-first usage
    - unsigned preview artifacts
    - expectation labeling
  - Updated changelog accordingly.
- Validation:
  - Documentation review completed.
- Risks:
  - Users may still confuse security warning with app defects if they skip README notes.
- Next:
  - After signing secrets are configured, publish a signed tag and remove reliance on workaround guidance in announcements.

## 2026-03-19 (CI Signing Strategy Adjustment)
- Date: 2026-03-19
- Goal: Keep release CI usable without Apple paid signing setup, while retaining a path to trusted macOS artifacts.
- Changes:
  - Updated publish workflow from strict signing enforcement to optional signing mode.
  - macOS release jobs now:
    - continue with unsigned artifacts when signing secrets are missing
    - import certificate and sign/notarize when secrets are present
  - Updated README and release checklist wording to mark signing secrets as optional/recommended (not mandatory for preview releases).
- Validation:
  - Workflow syntax reviewed and local frontend checks passed.
- Risks:
  - Unsigned macOS artifacts will still require user-side quarantine removal.
- Next:
  - Keep preview releases unsigned for now; enable signed release path later when Apple Developer subscription is ready.

## 2026-03-19 (Body Metrics Expansion + Preferences)
- Date: 2026-03-19
- Goal: Make body tracking more flexible by supporting richer metrics and per-metric visibility toggles.
- Changes:
  - Extended `BodyRecord` model and `body.csv` schema with optional numeric fields:
    - `bodyFat`
    - `muscleMass`
    - `chest`
    - `hip`
  - Updated body CSV parser/serializer:
    - canonical header now uses 8 columns
    - backward-compatible read for legacy 4-column files (`date,weight,waist,note`)
    - header-based parsing for mixed/partial columns
    - note-tail parsing keeps comma-separated text when users manually edit rows
  - Expanded Preferences body toggles for new metrics and wired UI visibility.
  - Refactored Body page to dynamic metric rendering:
    - form inputs
    - table columns
    - trend charts
  - Updated Dashboard body summary to respect expanded metric preferences.
  - Updated i18n messages (EN/ZH) for new labels and trend titles.
  - Updated Rust bootstrap default `body.csv` header for new profiles.
  - Updated README/data/QA/storage docs for the expanded schema.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (body csv suite expanded).
  - `npm run build` passed.
- Risks:
  - Existing historical `body.csv` files keep working, but first save in app normalizes header to the canonical expanded schema.
- Next:
  - Optional: add per-metric units and display formatting (e.g., `%` for body fat).

## 2026-03-19 (In-App Auto Update Integration)
- Date: 2026-03-19
- Goal: Add practical desktop auto-update capability with safe fallback when updater signing secrets are not configured.
- Changes:
  - Added Tauri updater/process plugins and permissions:
    - Rust deps: `tauri-plugin-updater`, `tauri-plugin-process`
    - capability grants: `updater:default`, `process:default`
  - Added updater config in `tauri.conf.json`:
    - endpoint points to GitHub latest release updater metadata URL
    - Windows install mode set to `passive`
    - pubkey can be injected at build time via `DAILYTRACK_UPDATER_PUBKEY`
  - Added frontend updater module:
    - `UpdaterContext` with persisted auto-check preference
    - check/update/install state management and progress display
    - install-and-relaunch flow using Tauri process plugin
  - Added update UI:
    - top banner for available update
    - Settings section for auto-check toggle, manual check, and install action
  - Added publish CI conditional updater mode:
    - if updater secrets are present, build uses `src-tauri/tauri.updater.conf.json` to generate updater artifacts
    - if missing, release still publishes installers but skips updater metadata/signatures
  - Updated README and release checklist for updater secrets and key-generation guidance.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - In-app auto-update requires updater signing secrets and a stable updater key strategy in CI.
  - Without updater secrets, UI controls remain available but updater checks are marked as not configured.
- Next:
  - Optional: expose release notes body in UI before install.
  - Optional: add staged rollout or pre-release channel switching.

## 2026-03-19 (Body Metric Unit + Display Format)
- Date: 2026-03-19
- Goal: Let each body metric use configurable unit and decimal display format.
- Changes:
  - Extended profile preferences schema for body metric display:
    - `unit` (string)
    - `decimals` (0-3)
  - Added body formatting utility module:
    - default metric units (`kg/cm/%`)
    - normalization/clamp logic for unit/decimals
    - reusable value/label formatter helpers
  - Preferences page now supports per-metric display editing:
    - keep metric toggle
    - edit unit text
    - choose decimal precision
  - Body page now applies configured display format to:
    - input placeholders
    - chart titles/axes/tooltips
    - history table labels/values
  - Dashboard body summary now applies configured display format and unit labels.
  - Added unit tests for display formatting helpers.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (4 files, 11 tests).
  - `npm run build` passed.
- Risks:
  - Very long custom unit strings are trimmed for display stability.
  - Existing profiles without display config are auto-normalized on next preferences load/save.
- Next:
  - Optional: add quick unit presets per metric (e.g., kg/lb, cm/in) to reduce manual typing.

## 2026-03-20 (v0.2.0 Hardening Pass)
- Date: 2026-03-20
- Goal: Improve sync/write reliability and document a concrete v0.2.0 roadmap.
- Changes:
  - Added profile-level sync mode in preferences (`watch` / `poll`) with schema versioning (`schemaVersion`).
  - Added Rust filesystem watcher commands:
    - `start_data_root_watch`
    - `stop_data_root_watch`
  - Added frontend `FilesystemWatchBridge` to translate Rust fs events into app live-sync events.
  - Updated Dashboard/Daily/Weekly/Body polling to act as fallback and adapt interval by sync mode.
  - Switched core text writes to atomic write strategy in Rust backend.
  - Upgraded export/import/migration commands to return structured copy summary counters.
  - Updated Settings UI to display copy summary after export/import/migration.
  - Added preferences normalization tests and new preferences schema documentation.
  - Updated README with Future TODOs and v0.2.0 milestone roadmap.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (5 files, 14 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Filesystem watcher behavior can vary by platform/filesystem type; polling fallback remains required.
  - Atomic replace path includes Windows-compatible fallback logic but should still be tested on native Windows runtime.
- Next:
  - Optional: add watcher integration tests (mocked callback + event filtering).
  - Optional: add native folder picker for Settings file path actions.

## 2026-03-20 (macOS Unsigned Install Helper)
- Date: 2026-03-20
- Goal: Reduce user friction for unsigned macOS app launch issues.
- Changes:
  - Added helper script: `scripts/macos/fix-dailytrack-quarantine.command`.
  - Added helper readme: `scripts/macos/README.md`.
  - Updated publish workflow to inject helper files into unsigned macOS DMG and re-upload patched DMG.
  - Updated README and release checklist with helper-script install flow.
- Validation:
  - Verified script has executable permission and targets `/Applications/dailytrack.app` by default.
  - Verified workflow has unsigned-macOS-only DMG patch step after tauri bundle upload.
- Risks:
  - Helper script is a workaround for unsigned builds and does not replace signing/notarization.
- Next:
  - Keep helper-in-DMG flow while unsigned preview distribution is still used.

## 2026-03-20 (Readable Release Notes + Release Skill)
- Date: 2026-03-20
- Goal: Improve release-note readability and create a reusable repo-level release skill.
- Changes:
  - Edited GitHub release notes for `v0.2.1` from default automated text to readable structured summary.
  - Added repo skill `skills/dailytrack-release` with:
    - `SKILL.md` workflow definition
    - release runbook/check references
    - release notes template
    - executable draft generator script (`scripts/make_release_notes.sh`)
  - Updated `AGENTS.md`, `README.md`, and `CHANGELOG.md` to document the new release skill workflow.
- Validation:
  - Verified release notes update via `gh release view v0.2.1 --json body`.
  - Ran `skills/dailytrack-release/scripts/make_release_notes.sh v0.2.0 v0.2.1` and confirmed expected markdown output.
- Risks:
  - Skill validator (`quick_validate.py`) could not run locally because Python dependency `PyYAML` is not installed in current environment.
- Next:
  - Optional: install `PyYAML` and run strict skill validation.

## 2026-03-20 (WebDAV Cloud Snapshot Sync)
- Date: 2026-03-20
- Goal: Add practical WebDAV cloud backup/sync while preserving markdown/csv local-first architecture.
- Changes:
  - Added Rust WebDAV module `src-tauri/src/webdav.rs` with commands:
    - `get_webdav_config`
    - `save_webdav_config`
    - `test_webdav_connection`
    - `webdav_list_snapshots`
    - `webdav_push_snapshot`
    - `webdav_pull_snapshot`
    - `webdav_delete_snapshot`
  - Added WebDAV snapshot model:
    - remote `meta.json` + `snapshots/<id>.zip`
    - checksum verification (`sha256`) on pull
    - snapshot pruning by `maxSnapshots`
    - optional local backup before pull (`dailytrack-webdav-backup-<timestamp>`)
  - Added frontend API wrappers and new `features/webdav` modules:
    - config normalization/format helpers
    - `WebdavSyncBridge` for optional interval auto-push
  - Extended Settings page with full WebDAV controls:
    - save config
    - test connection
    - push now
    - pull latest/selected
    - snapshot refresh/list/delete
  - Added EN/ZH i18n copy for WebDAV settings workflow.
  - Updated README/FS storage/QA docs and changelog for WebDAV behavior.
- Validation:
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - `npm run lint` passed.
  - `npm run test` passed (5 files, 14 tests).
  - `npm run build` passed.
- Risks:
  - First release targets common WebDAV implementations (e.g., Nextcloud); compatibility with all servers is not guaranteed.
  - Credentials are stored in local app config file by current product choice (not keychain-backed yet).
- Next:
  - Optional: add profile-scoped sync mode and/or per-snapshot diff preview before pull.
  - Optional: add keychain-backed credential storage for stronger local security.

## 2026-03-20 (WebDAV Config Autosave UX)
- Date: 2026-03-20
- Goal: Remove manual save friction in WebDAV settings and prevent stale-config test failures.
- Changes:
  - Updated Settings WebDAV section to debounced autosave (700ms) on config edits.
  - Removed manual "Save WebDAV Config" button from the UI.
  - Added pre-action force-save guard before:
    - `Test Connection`
    - `Refresh Snapshots`
    - `Push Now`
    - `Pull Latest/Selected`
    - `Delete Selected`
  - Added autosave status copy (EN/ZH): autosaving/autosaved.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Frequent edits trigger frequent config writes (debounced) by design.
- Next:
  - Optional: surface last-saved timestamp in WebDAV settings for clearer state visibility.

## 2026-03-20 (Structured Profile Template Editing + Apply Actions)
- Date: 2026-03-20
- Goal: Make profile template customization practical in-app and solve "template changed but today's note not updated" confusion.
- Changes:
  - Profiles current-template editor now defaults to `Structured` mode.
  - Added structured editing for daily and weekly templates:
    - editable checklist item text
    - add/remove checklist items
    - editable weekly reflection fields
  - Kept `Raw Markdown` mode as fallback for direct text editing.
  - Added one-click overwrite actions in Profiles:
    - apply current daily template to today's daily file
    - apply current weekly template to current week's weekly file
  - Added EN/ZH copy for structured mode hints, add/remove actions, apply confirmations, and success/error messages.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (5 files, 14 tests).
  - `npm run build` passed.
- Risks:
  - Apply actions are overwrite operations; users can replace existing note content if used accidentally (confirmation prompt is provided).
- Next:
  - Optional: add preview diff before overwrite apply.

## 2026-03-20 (Create Profile Structured Template Editor + README Reorg)
- Date: 2026-03-20
- Goal: Align create-profile template editing UX with current-profile template editing and improve README first-contact clarity.
- Changes:
  - Profiles `Create Profile` section now supports the same `Structured` / `Raw Markdown` template editing mode toggle.
  - Structured mode in create-profile now supports:
    - daily `Daily Core` and `Optional` item add/remove/edit
    - weekly section checklist item add/remove/edit
    - weekly reflection field editing
  - Added parse-failure message for create-profile structured mode fallback to raw markdown repair.
  - Reorganized README to a user-first structure:
    - clear release download path at top
    - installer selection by OS/CPU
    - compact HTML screenshot gallery
    - cleaner separation of user usage vs maintainer release workflow
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (5 files, 14 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Structured editing remains schema-driven; non-standard template markdown may still require raw-mode fixes.
- Next:
  - Optional: refactor duplicated structured-editor logic into reusable components/hooks.

## 2026-03-21 (Mobile Shell + WebDAV Realtime Sync)
- Date: 2026-03-21
- Goal: Start mobile adaptation and ship first usable WebDAV realtime sync workflow.
- Changes:
  - Added responsive app shell navigation:
    - desktop sidebar on wide screens
    - mobile bottom navigation on narrow screens
    - added `Sync` entry in both nav systems
  - Replaced placeholder Sync page with realtime controls:
    - load sync status (pending changes / conflicts / last push / last pull)
    - run `Sync Both`, `Push Only`, `Pull Only`
    - list unresolved conflicts and resolve with `Keep Local` / `Apply Remote` / `Mark Resolved`
  - Implemented Rust WebDAV realtime engine:
    - manifest + file transport paths (`realtime/manifest.json`, `realtime/files/*`)
    - local scan/hash comparison against base+remote state
    - conflict detection and conflict copy materialization under local `conflicts/`
    - new Tauri commands for status/sync/conflicts/resolve
  - Updated frontend FS API wrappers and bridge behavior:
    - `WebdavSyncBridge` now runs realtime sync loop instead of snapshot auto-push
    - visibility-triggered sync and event emission on successful updates
  - Added EN/ZH i18n copy for Sync status/actions/conflict UI.
  - Updated README/architecture/storage/QA docs to reflect realtime sync model.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (5 files, 14 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Current realtime loop is periodic and manifest-based; high-churn multi-device edits can still generate frequent conflicts.
  - Conflict resolution currently depends on local conflict copy files and does not provide an inline textual diff view.
- Next:
  - Add batched backoff/retry for transient WebDAV errors.
  - Add optional conflict diff preview for easier manual resolution.

## 2026-03-21 (Sync UX + Mobile Access Polish)
- Date: 2026-03-21
- Goal: Improve realtime sync usability and remove mobile route access gaps.
- Changes:
  - Enhanced Sync page interaction flow:
    - added `Refresh` action and visible-status auto-refresh (10s, visible window only)
    - added conflict metadata (`createdAt`) display
    - added expand/collapse conflict preview showing local vs remote text snippets
    - added per-conflict resolving lock to prevent repeated action clicks
  - Added mobile quick menu component in top mobile header:
    - exposes secondary routes not present in bottom nav
    - closes automatically after route click
  - Added EN/ZH i18n keys for new mobile menu and sync UX copy.
  - Updated README/Architecture/Changelog docs for these behavior changes.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (5 files, 14 tests).
  - `npm run build` passed.
- Risks:
  - Conflict text preview only supports UTF-8 text files; binary files show read errors by design.
- Next:
  - Optional: add line-level diff view for markdown/csv conflicts.
  - Optional: add filter/search for large conflict lists.

## 2026-03-21 (Mobile Menu Dismiss + Sync State Cleanup)
- Date: 2026-03-21
- Goal: Smooth small UX rough edges after the previous sync/mobile batch.
- Changes:
  - Mobile menu now supports outside-tap dismiss via fixed backdrop layer.
  - Sync conflict page now clears stale expanded/preview state when unresolved conflict list changes.
- Validation:
  - `npm run lint` passed.
  - `npm run build` passed.
- Risks:
  - None significant; behavior is UI-local.
- Next:
  - Optional: add keyboard accessibility refinements for mobile menu and conflict preview sections.

## 2026-03-21 (Sync Line Diff View)
- Date: 2026-03-21
- Goal: Upgrade conflict preview from plain text blocks to line-level diff for faster manual resolution.
- Changes:
  - Added reusable line diff utility:
    - `src/lib/diff/lineDiff.ts`
    - LCS-based line matching
    - outputs side-by-side diff rows with line numbers and row kind (`same/add/remove`)
  - Added unit tests for line diff utility (`src/lib/diff/lineDiff.test.ts`).
  - Sync page conflict preview now includes:
    - line-level diff table with color-highlight legend
    - continued raw local/remote preview fallback under diff
  - Added EN/ZH i18n keys for diff labels and empty-diff messaging.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - Diff is line-based only and does not do token-level or semantic markdown diff.
- Next:
  - Optional: add hide-unchanged-lines mode for large conflicts.

## 2026-03-21 (Mobile Layout Pass for Dashboard/Body/Settings)
- Date: 2026-03-21
- Goal: Improve narrow-window/mobile usability for high-frequency pages.
- Changes:
  - Dashboard:
    - reduced card spacing and heading/link sizes on mobile
    - adjusted grid breakpoints to avoid cramped 3-column behavior too early
  - Body:
    - compacted form grid and action rows for mobile
    - improved chart rendering with safer margins/ticks and adaptive heights
    - added mobile card-list history view (`md:hidden`) while keeping desktop table
  - Settings:
    - tightened section padding and added stronger path wrapping
    - key action buttons switched to full-width on mobile (`sm:w-auto` fallback)
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - Body mobile history card layout can become long for very large enabled metric sets; still scroll-based by design.
- Next:
  - Optional: add compact/comfortable density toggle for user preference.

## 2026-03-21 (Sync Diff: Only-Changes Mode)
- Date: 2026-03-21
- Goal: Improve conflict review efficiency by allowing users to focus only on changed lines.
- Changes:
  - Added `Only show changes` toggle on Sync conflict diff view.
  - Default state is enabled, hiding unchanged rows in diff table.
  - Added fallback empty-state copy when preview has no changed lines in current window.
  - Added EN/ZH i18n for new diff mode copy.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - When preview text is truncated, hidden unchanged rows can make context feel sparse; raw preview remains available below diff.
- Next:
  - Optional: add one-click expand context lines around each changed block.

## 2026-03-21 (CI: Android APK + Publish Speedup)
- Date: 2026-03-21
- Goal: Add APK CI output path and reduce release workflow build latency.
- Changes:
  - Added Android APK workflow (`.github/workflows/android-apk.yml`):
    - supports `workflow_dispatch` (debug/release choice)
    - supports tag-triggered builds (`v*`)
    - uploads APK artifacts and can upload APKs to tagged GitHub releases
  - Optimized existing release workflow (`publish.yml`):
    - introduced `verify-frontend` job (single-run lint/test/build on ubuntu)
    - uploaded `dist/` as artifact and reused it in matrix jobs
    - added `src-tauri/tauri.ci.conf.json` to skip repeated frontend rebuild in matrix packaging
  - Added local faster iteration script: `npm run build:desktop:fast`.
  - Updated README / DEV_SETUP / RELEASE_CHECKLIST / CHANGELOG for new workflow and speed guidance.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Android workflow depends on SDK/NDK package availability on GitHub-hosted runners; occasional upstream package mirror instability may impact run reliability.
- Next:
  - Optional: add release-note auto-append for APK artifact names and checksums.

## 2026-03-21 (Android Build Speed Tuning)
- Date: 2026-03-21
- Goal: Reduce Android APK CI turnaround time while preserving release flexibility.
- Changes:
  - APK workflow now uses Rust cache (`swatinem/rust-cache`) and Gradle cache (`actions/cache`).
  - Default debug builds now target `aarch64` only for faster CI feedback.
  - Release builds keep `aarch64 + x86_64` target coverage.
  - Updated README and DEV_SETUP to document speed profile and local commands.
- Validation:
  - Workflow YAML schema parse passed locally.
- Risks:
  - Cache misses on first run or cache invalidation events will still incur full compile times.
- Next:
  - Optional: add a `full_matrix` input switch to explicitly force all ABIs for debug validation runs.

## 2026-03-21 (Versioned Changelog Governance)
- Date: 2026-03-21
- Goal: Enforce release-version-based changelog management and align release skill/runbook.
- Changes:
  - Reworked `CHANGELOG.md` from a single large `Unreleased` bucket into versioned sections:
    - `## [0.1.0]` through `## [0.4.1]`
    - retained `## [Unreleased]` only for pending, untagged work.
  - Updated `AGENTS.md` with explicit changelog versioning rules and DoD wording.
  - Updated release skill docs:
    - `skills/dailytrack-release/SKILL.md`
    - `skills/dailytrack-release/references/runbook.md`
    to require moving `Unreleased` entries into `## [X.Y.Z] - YYYY-MM-DD` before tagging.
  - Updated `docs/RELEASE_CHECKLIST.md` and `README.md` release snippet to reflect the same process.
- Validation:
  - Reviewed heading structure with `rg '^## \[' CHANGELOG.md` to confirm version split.
  - Verified docs consistency across AGENTS, release skill, runbook, README, and release checklist.
- Risks:
  - Historic entries before this migration are summarized; detailed per-commit nuance still lives in GitHub release pages and devlog history.
- Next:
  - Optional: add changelog compare links (`[0.4.1]: ...`) at file bottom for stricter Keep-a-Changelog style.

## 2026-03-21 (v0.4.2 Release Prep: Updater + WebDAV Pull + Android CI)
- Date: 2026-03-21
- Goal: Finalize and release a stability/operability update focused on updater UX, WebDAV auto-pull behavior, Android icon correctness, and APK release sizing.
- Changes:
  - Updater:
    - Added runtime/platform support detection for in-app updater.
    - Disabled updater check flow on mobile builds to prevent false "check failed" paths.
    - Updated settings/i18n to show `supported` vs `configured` clearly.
  - WebDAV:
    - Added `autoPullEnabled` config field (default off).
    - Added settings toggle for auto pull.
    - Implemented 30s throttled pull loop triggered by data-change events, with guardrails to avoid sync-event feedback loops.
  - Android icon:
    - Added manifest-driven icon generation workflow and Android-specific foreground/source assets.
    - Regenerated launcher assets to avoid cropped wordmark in round launcher icons.
  - CI / build size:
    - `publish.yml` Android APK build switched from debug to release.
    - `android-apk.yml` default mode switched to release; targets simplified to `aarch64` for faster runs.
    - Added Rust release profile size optimizations in `src-tauri/Cargo.toml`.
  - Release process:
    - Updated `CHANGELOG.md` with `0.4.2` section.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - Workflow YAML parsed successfully (`publish.yml`, `android-apk.yml`).
- Risks:
  - WebDAV auto-pull currently polls every 30s when pending; future optimization could use file-level change batching metadata.
  - Android in-app updater remains unsupported by this release path; release delivery is artifact-based.
- Next:
  - Trigger `v0.4.2` publish workflow and verify release assets + updater metadata.

## 2026-03-22 (v0.4.3 Release Prep: Android Signing/Icon + WebDAV Polling)
- Date: 2026-03-22
- Goal: Ship a follow-up release that fixes Android distribution quality and improves cross-device pull behavior for WebDAV sync.
- Changes:
  - WebDAV:
    - Auto-pull loop changed to time-based polling (no local-change dependency).
    - Added user-configurable auto-pull interval in seconds.
  - Android CI distribution:
    - Added Android keystore-based release APK signing flow via repository secrets.
    - Updated asset collection to prefer signed APK outputs.
    - Removed unsigned APK asset from `v0.4.2` release after signed replacement upload.
  - Android icon pipeline:
    - Added workflow step to re-apply branded launcher resources after `tauri android init` so generated Android project does not regress to default icon assets.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - Workflow YAML parse passed for both Android release workflows.
- Risks:
  - Android install/update behavior still depends on device-side trust settings and OEM package installer behavior.
  - Auto-pull polling interval that's too short can increase remote requests and battery/network usage.
- Next:
  - Release `v0.4.3` and verify Android release asset installability and launcher icon correctness on-device.

## 2026-03-22 (Roadmap Refresh)
- Date: 2026-03-22
- Goal: Convert README future TODO bullets into versioned milestones for clearer iterative planning.
- Changes:
  - Replaced the generic `Future Roadmap` bullet list in `README.md` with milestone sections:
    - `v0.5.0` Sync Reliability
    - `v0.6.0` Profiles and Templates
    - `v0.7.0` Mobile Usability
    - `v0.8.0` Reports and AI Assist
    - `v1.0.0` Distribution and Ops
  - Added a short scope note to keep milestones adaptable to user feedback and solo-dev iteration.
- Validation:
  - Reviewed rendered markdown structure to ensure roadmap is easily scannable on GitHub.
- Risks:
  - Milestones are planning targets; dates and scope can shift as higher-priority bugfix work appears.
- Next:
  - Track each milestone as issue groups and map into release notes as implementation progresses.

## 2026-03-22 (AGENTS Roadmap Rule Sync)
- Date: 2026-03-22
- Goal: Keep agent-facing collaboration rules aligned with the new versioned roadmap workflow.
- Changes:
  - Updated `AGENTS.md` with `Roadmap and Milestone Rules`:
    - roadmap remains version-based in `README.md`
    - each milestone keeps small, practical scope
    - roadmap progress is logged in devlog
    - shipped milestone outcomes are reflected in versioned changelog sections
- Validation:
  - Reviewed `AGENTS.md` and `README.md` for consistent roadmap language and release flow.
- Risks:
  - Rules may still need refinement as release cadence and milestone size evolve.
- Next:
  - Apply the same pattern to upcoming `v0.5.0` execution and release notes.

## 2026-03-22 (v0.5.0 Kickoff: Sync Reliability Batch 1)
- Date: 2026-03-22
- Goal: Start `v0.5.0` with practical sync reliability improvements in the realtime sync view.
- Changes:
  - Upgraded Sync page status visibility:
    - sync health state (`healthy` / `degraded` / `syncing`)
    - error category classification
    - last sync timestamp
    - next auto-pull countdown (when auto-pull is enabled)
  - Added local diagnostics export from Sync page:
    - outputs JSON under `<data-root>/sync-diagnostics/`
    - includes status/conflicts/config/runtime context
    - redacts stored WebDAV password in exported payload
  - Added batch conflict actions:
    - select all / clear selection
    - batch keep local / apply remote / mark resolved
    - summary feedback for partial failures
  - Added safer confirmation dialogs before applying remote content (single + batch).
  - Added i18n strings (EN/ZH) for all new sync controls and states.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - Error category classification is heuristic string matching and may need refinement as more real-world error samples appear.
  - Batch resolve currently executes sequentially; large conflict sets can take noticeable time.
- Next:
  - Continue `v0.5.0` batch 2: add richer retry telemetry and optional one-click conflict strategy presets.

## 2026-03-22 (v0.5.0 Sync Reliability Batch 2)
- Date: 2026-03-22
- Goal: Deliver retry telemetry visibility and one-click conflict strategy presets for faster recovery operations.
- Changes:
  - Extended realtime sync telemetry in Rust state/status:
    - `lastAttemptAt`
    - `lastSuccessAt`
    - `lastFailureAt`
    - `consecutiveFailures`
    - `totalSuccesses`
    - `totalFailures`
  - Updated sync failure persistence path to count failures and record last failure timestamp.
  - Updated sync success path to record success counters/timestamps and reset consecutive failures.
  - Added one-click conflict strategy preset actions in Sync page:
    - apply to all unresolved: keep local / apply remote / mark resolved
    - with explicit confirmation for destructive strategies
  - Added corresponding EN/ZH i18n labels for telemetry + preset controls.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Telemetry is persisted per data-root hash state file; manual state-file deletion resets counters.
  - Preset actions still execute sequentially and can be slow for very large conflict sets.
- Next:
  - Continue `v0.5.0` batch 3: expose quick retry controls and optional backoff hints based on consecutive failure count.

## 2026-03-22 (v0.5.0 Sync Reliability Batch 3)
- Date: 2026-03-22
- Goal: Improve operator guidance during degraded sync periods with visible retry timing and fast recovery action.
- Changes:
  - Added retry backoff visibility in Sync page status:
    - computed backoff interval from consecutive failure count (capped)
    - next retry countdown derived from last failure timestamp
  - Added quick retry button when sync health is degraded (direct `Sync Both` trigger).
  - Added EN/ZH i18n for retry backoff, next retry, quick retry, and retry hint text.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - Backoff hint is recommendation-only and does not yet enforce automatic retry throttling.
- Next:
  - Continue `v0.5.0` batch 4: evaluate conflict-resolution throughput improvements for large conflict sets.

## 2026-03-22 (v0.5.0 Sync Reliability Batch 4)
- Date: 2026-03-22
- Goal: Improve large-conflict throughput and state consistency for batch conflict operations.
- Changes:
  - Added new backend command:
    - `webdav_realtime_conflicts_resolve_batch`
    - resolves multiple conflict IDs in one call with strategy + per-ID failure reporting
  - Refactored conflict-apply logic into shared helper in Rust to keep single and batch resolve behavior aligned.
  - Frontend batch/preset actions now call the new single batch command instead of looped per-conflict invokes.
  - Added typed API client surface for batch resolve result (`requested`, `resolved`, `failedIds`, `strategy`).
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Batch resolve currently writes final state once per batch; if process crashes mid-batch, partial local file effects may exist before state save.
- Next:
  - Continue `v0.5.0` batch 5: add optional dry-run summary for destructive batch strategies before execution.

## 2026-03-22 (v0.5.0 Sync Reliability Batch 5)
- Date: 2026-03-22
- Goal: Add preflight visibility for destructive conflict actions to reduce accidental data loss.
- Changes:
  - Added dry-run summaries for conflict strategy actions:
    - selection-level dry-run preview (apply-remote expected overwrite/delete counts)
    - preset-all dry-run preview (apply-remote expected overwrite/delete counts)
  - Updated conflict confirm dialogs to use dry-run-aware messaging:
    - apply-remote confirms with summarized overwrite/delete counts
    - mark-resolved confirms with affected conflict count
  - Removed obsolete sync confirmation copy keys no longer used after dry-run unification.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (6 files, 16 tests).
  - `npm run build` passed.
- Risks:
  - Dry-run summary is derived from current in-memory conflict list; if list changes externally between preview and click, actual result may differ slightly.
- Next:
  - Start release-candidate stabilization for `v0.5.0` (final QA pass + changelog version cut prep).

## 2026-03-22 (v0.5.0 Release Prep)
- Date: 2026-03-22
- Goal: Move completed sync-reliability work from `Unreleased` into a versioned release section and ensure release readiness.
- Changes:
  - Cut `CHANGELOG.md` section: `## [0.5.0] - 2026-03-22`.
  - Restored `Unreleased` placeholder guidance for next development cycle.
  - Confirmed `v0.5.0` scope focuses on sync reliability:
    - health/error telemetry visibility
    - batch/preset conflict handling
    - dry-run safety summary
    - diagnostic export
    - retry guidance and quick retry action
    - backend batch resolve optimization
- Validation:
  - Latest code state already validated with:
    - `npm run lint`
    - `npm run test -- --run`
    - `npm run build`
    - `cargo check --manifest-path src-tauri/Cargo.toml`
- Risks:
  - macOS artifacts remain unsigned in current distribution mode and still require Gatekeeper workaround flow.
- Next:
  - Tag and publish `v0.5.0`, then verify release artifacts/update metadata.

## 2026-03-22 (v0.6.0 Batch: Template Catalog Externalization + Profile Template Update)
- Date: 2026-03-22
- Goal: Start v0.6.0 by making template goals easy to edit in-repo and adding safe profile template update workflows.
- Changes:
  - Externalized template presets from TS source into repository config:
    - Added `config/template-presets.json` manifest (preset metadata + variant file paths).
    - Added markdown template files under `config/templates/<preset>/<en|zh>/{daily,weekly}.md`.
    - Added runtime catalog loader/validator (`templateCatalog`) with fallback behavior.
  - Added reusable template schema helpers (`templateSchema`) for template parse/serialize/normalize.
  - Added profile template metadata service (`templateMeta.service`):
    - reads/writes `templates/template-meta.json`
    - tracks `presetId`, `templateLanguage`, `lastAppliedMode`, `lastAppliedAt`.
  - Added template update computation service (`templateUpdate.service`):
    - supports `merge` (default) and `overwrite`
    - dry-run preview output for daily/weekly/reflection deltas
    - schema guardrails for known daily/weekly headings.
  - Updated onboarding/profile creation/data-root flows to persist template metadata on preset apply.
  - Extended Profiles page:
    - template source metadata display
    - preset/language/mode selector for updating current templates
    - preview + apply update actions with overwrite confirmation
    - update summaries shown in-page.
  - Added tests:
    - `templateCatalog.test.ts`
    - `templateUpdate.service.test.ts`.
  - Updated docs (`README`, `DATA_MODEL`, `FS_STORAGE`, `QA_CHECKLIST`, `CHANGELOG`).
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (8 files, 22 tests).
  - `npm run build` passed.
- Risks:
  - `merge` matching is text-normalization based; semantically-similar but differently-worded items may still be treated as distinct.
  - Template update flow currently applies on explicit user action; no automatic migration for all existing profiles.
- Next:
  - Continue v0.6.0 by reducing Profiles page duplication (shared structured template editor component/hook).
  - Add template-update QA pass on real profile data and capture edge-case examples.

## 2026-03-22 (v0.6.0 Release Prep + Bilingual Preset Naming Polish)
- Date: 2026-03-22
- Goal: Finalize v0.6.0 release docs and improve bilingual template naming/description UX before tagging.
- Changes:
  - Moved v0.6.0 items from `CHANGELOG` `Unreleased` into versioned section `## [0.6.0] - 2026-03-22`.
  - Completed bilingual preset descriptions in `config/template-presets.json` for all preset IDs.
  - Updated Profiles page behavior:
    - default template language now follows current UI language (`zh` UI defaults to `zh` template language)
    - template source display now shows localized preset label + preset id.
  - Updated ZH i18n language option labels from `English/中文` to `英文/中文` in template/onboarding selectors.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (8 files, 22 tests).
  - `npm run build` passed.
- Risks:
  - Localized label display depends on preset metadata completeness; missing labels would fall back to preset ID-based behavior.
- Next:
  - Tag and publish `v0.6.0` via release workflow and update GitHub release notes to readable summary.

## 2026-03-22 (v0.7.0 Batch: Mobile Usability + Template-Delta Focus Views)
- Date: 2026-03-22
- Goal: Start v0.7.0 by improving narrow-screen/mobile usability and expanding template-based "only show changes" workflows.
- Changes:
  - Preferences schema upgraded to v3:
    - added `ui.showOnlyChanges.{daily,weekly,body}`
    - added `ui.mobile.showSyncBanner`
    - normalization now writes current schema version on load/save.
  - Added template/body diff utilities + tests:
    - `daily.diff` for Daily checklist/one-line template deltas
    - `weekly.diff` for Weekly checklist/reflection template deltas
    - `body.diff` for non-empty metric/note detection.
  - Daily/Weekly/Body pages:
    - added "Only show changes" toggle in structured views
    - toggle persists to per-profile preferences
    - added empty-state hints and template-diff fallback copy.
  - Mobile shell + sync visibility:
    - shell switched to fixed-height layout with independent main-content scrolling
    - added mobile sync status banner (offline/pending/conflicts/next pull) with quick jump to `/sync`
    - added mobile sync status polling hook (`useSyncStatus`).
  - Body chart rendering:
    - replaced pure `ResponsiveContainer` path with measured container sizing to improve narrow-window rendering stability.
  - Updated docs:
    - `CHANGELOG` Unreleased
    - `README` feature list
    - `ARCHITECTURE`, `DATA_MODEL`, `PREFERENCES_SCHEMA`, `QA_CHECKLIST`.
- Validation:
  - `npm run test -- --run` passed (11 files, 28 tests).
  - `npm run build` passed.
  - `npm run lint` passed.
- Risks:
  - Template-delta mode currently compares against current profile templates; if templates change later, historical notes' "change" classification can shift.
  - `useSyncStatus` currently polls status periodically in shell; if users disable WebDAV this is still a lightweight read path and may be optimized further.
- Next:
  - Add optional timestamp/source hint in mobile sync banner for clearer trust signal.
  - Consider extracting shared "show only changes" toggle UI into reusable component to reduce page duplication.

## 2026-03-22 (v0.7.0 Release Prep)
- Date: 2026-03-22
- Goal: Prepare versioned release records and publish `v0.7.0` with validated artifacts.
- Changes:
  - Moved v0.7.0 user-facing entries from `CHANGELOG` `Unreleased` to `## [0.7.0] - 2026-03-22`.
  - Restored `Unreleased` placeholder guidance for subsequent development.
  - Confirmed release scope focuses on mobile usability + template-delta workflow expansion.
- Validation:
  - `npm run test -- --run` passed.
  - `npm run lint` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - macOS artifacts remain unsigned in current distribution mode and may still require user-side Gatekeeper workaround.
- Next:
  - Tag and publish `v0.7.0`, then verify cross-platform release assets and update release summary to readable notes.

## 2026-03-22 (v0.7.1 Fix Release: Sync Countdown + Diagnostics Export)
- Date: 2026-03-22
- Goal: Fix reported Sync page issues before publishing `v0.7.1`.
- Changes:
  - Fixed WebDAV "Next Auto Pull" countdown logic:
    - countdown now uses `lastAttemptAt` in addition to `lastPullAt/lastPushAt`
    - when no backend timestamp exists yet, UI now uses a stable local anchor instead of staying at "Now".
  - Fixed diagnostics export path validation in Rust:
    - writable-path validation now allows writing files under new nested directories inside data root
    - keeps path traversal protections by validating nearest existing ancestor + relative components.
  - Updated release docs for `v0.7.1` in `CHANGELOG`.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (11 files, 28 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - macOS distribution remains unsigned in current public workflow; Gatekeeper workaround may still be required for some users.
- Next:
  - Tag and publish `v0.7.1`, verify release artifacts, then update release summary to readable notes.

## 2026-03-23 (Icon Standardization: macOS + Android Launcher)
- Date: 2026-03-23
- Goal: Fix launcher icon visual inconsistency on macOS and Android.
- Changes:
  - Reworked icon manifest configuration:
    - removed global `bg_color` flattening for default icon generation
    - added explicit `android_bg` source
    - increased `android_fg_scale` from `78` to `92` for better adaptive-icon occupancy.
  - Rebuilt icon source assets:
    - regenerated `dailytrack-app-icon.png` with normalized transparent padding for desktop/mobile launcher usage
    - regenerated `dailytrack-android-fg.png` with improved occupancy
    - added `dailytrack-android-bg.png` for adaptive icon background.
  - Regenerated Tauri icon outputs via `npm run icon:generate` (desktop + Android + iOS icon variants).
  - Updated icon docs (`src-tauri/icons/README.md`) to include Android background source.
- Validation:
  - Verified desktop PNG icon corners are transparent (`icon.png`, `128x128.png`).
  - Verified Android adaptive assets regenerated (`ic_launcher_foreground/background/round`) with updated scaling.
  - Checked generated dimensions and alpha channels via `sips`/`magick`.
- Risks:
  - Final launcher visual perception still depends on OEM launcher mask/style and app grid size.
- Next:
  - Build and test on macOS Dock + Android launcher device to confirm final perceived size consistency.

## 2026-03-23 (v0.7.2 Batch: Perf Smoothing + Launcher Icon Normalization)
- Date: 2026-03-23
- Goal: Ship `v0.7.2` focusing on perceived stutter reduction and launcher icon consistency.
- Changes:
  - Performance/resume smoothing:
    - moved mobile sync status polling/rendering out of global `AppShell` into a dedicated mobile-only banner component.
    - added sync scheduling guardrails in `WebdavSyncBridge` (initial/visibility delay + minimum sync gap + hidden-state pull suppression) to reduce foreground resume burst load.
  - Launcher icon normalization:
    - adjusted icon manifest to use explicit Android background and larger adaptive foreground scale.
    - regenerated desktop/mobile icon assets with transparent desktop corners and improved Android adaptive occupancy.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Android launcher rendering still varies by OEM mask/shape policy.
- Next:
  - Tag and publish `v0.7.2`, then verify release assets and update release notes.

## 2026-03-23 (Android Icon Cropping Follow-up)
- Date: 2026-03-23
- Goal: Fix launcher text clipping (`d`/`k` bottom cut) on Android home screen.
- Changes:
  - Kept adaptive manifest foreground scale at a balanced value (`android_fg_scale: 86`) for legacy launcher outputs.
  - Rebuilt `dailytrack-android-fg.png` with smaller occupancy and upward offset to keep content inside common adaptive-mask safe zones.
  - Regenerated Android launcher outputs (`mipmap-*/ic_launcher*`) from updated foreground source.
- Validation:
  - `xxxhdpi` adaptive foreground trim changed from `384x380+24+26` to `322x319+55+45` (more bottom/edge safety).
  - Confirmed regenerated `ic_launcher_foreground`, `ic_launcher`, and `ic_launcher_round` assets across densities.
- Risks:
  - Some OEM masks can still differ; final visual scale may vary slightly by launcher.
- Next:
  - Verify on-device launcher rendering in target Android model; if still tight, reduce foreground width one more step.

## 2026-03-24 (Android New-day Dashboard Resilience)
- Date: 2026-03-24
- Goal: Fix Android dashboard failure when opening app on a new day before manual desktop sync.
- Changes:
  - Added built-in fallback templates in daily/weekly services when local `templates/daily.md` or `templates/weekly.md` read fails.
  - Added safe fallback in body service: return empty records (with warning log) when `body.csv` read fails.
  - Updated dashboard loading from `Promise.all` to `Promise.allSettled`:
    - keep Today/This Week as required
    - tolerate body/recent list read failures without failing whole dashboard.
  - Added console diagnostics logs for dashboard/data-read failures to improve future debugging on Android.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (11 files, 28 tests).
  - `npm run build` passed.
- Risks:
  - Root-cause source file corruption/deletion still needs separate telemetry to pinpoint exact origin.
- Next:
  - Observe Android next-day cold start behavior; if issue repeats, capture Sync diagnostics export and compare template/body file presence on-device.

## 2026-03-24 (Android Body Input Responsiveness)
- Date: 2026-03-24
- Goal: Reduce body-form input lag after app resume from background (Android).
- Changes:
  - Added body-form interaction guard to pause body polling while any input is focused or recently edited.
  - Added visibility-resume grace window before body polling restarts, reducing resume-time state contention.
  - Prevented polling-driven “updated from disk” message churn during active input.
  - Memoized body-page metric/record derived collections to reduce per-keystroke recomputation pressure.
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
- Risks:
  - External body-file changes may appear up to ~2s later when user is actively typing (intentional tradeoff for input responsiveness).
- Next:
  - If lag still appears on low-end Android devices, split chart/history into memoized subcomponents and/or virtualize long history lists.

## 2026-03-24 (Release v0.7.3)
- Date: 2026-03-24
- Goal: Prepare and publish `v0.7.3` with Android reliability and responsiveness fixes.
- Included commits:
  - `800257a` fix(android-icon): prevent launcher text clipping.
  - `d3af506` fix(android): harden dashboard load on new-day file gaps.
  - `93ecf4b` fix(android): reduce body input lag after app resume.
- Release prep:
  - Moved unreleased entries into `CHANGELOG.md` section `## [0.7.3] - 2026-03-24`.
  - Kept `Unreleased` section reserved for future work.
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Next:
  - Push tag `v0.7.3`, monitor `publish.yml`, then finalize readable GitHub release summary.

## 2026-03-24 (v0.8.0 UI/UX Overhaul Batch)
- Date: 2026-03-24
- Goal: Implement the v0.8.0 visual + information-architecture refresh (Apple-inspired Calm Glass direction).
- Changes:
  - Added centralized navigation model (`src/app/navigation.ts`) and reusable glyph icons (`src/components/NavGlyph.tsx`).
  - Rebuilt shell structure with layered desktop/mobile navigation and removed always-on technical status lines from the main workspace.
  - Added UI design tokens (`src/styles/tokens.css`) and shared visual classes (`dt-panel`, `dt-input`, `dt-btn`, etc.).
  - Restyled core screens (Dashboard, Daily, Weekly, Body, list pages, Reports, Preferences, Sync, Profiles, Settings) and onboarding modal.
  - Added Settings diagnostics block for root/profile/init runtime visibility.
  - Added i18n strings for new nav grouping and diagnostics labels (EN/ZH).
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
- Risks:
  - Visual churn is broad; minor spacing/style inconsistencies may remain in low-frequency interactions.
- Next:
  - Capture new screenshots and update README visual section for v0.8.0 release notes.

## 2026-03-24 (Tutorial Expansion)
- Date: 2026-03-24
- Goal: Make in-app tutorial more detailed and include complete feature explanation.
- Changes:
  - Extended tutorial from a short 5-step nav guide to a richer multi-step walkthrough covering Dashboard, lists, Reports, Sync, Preferences, language switch, and Settings.
  - Added feature-overview steps that explain source-of-truth model, complete feature map, and suggested daily workflow.
  - Added additional tour anchors in centralized navigation config so highlights can target more routes.
  - Updated bilingual tutorial copy (EN/ZH) with more practical, product-complete guidance.
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
- Risks:
  - On some narrow/mobile contexts, certain target anchors may be temporarily out of view during guide steps; tutorial still remains navigable.
- Next:
  - Optionally add a dedicated “Feature Guide” static page for persistent, non-overlay documentation.

## 2026-03-24 (Release v0.8.0)
- Date: 2026-03-24
- Goal: Publish `v0.8.0` with UI/UX overhaul, tutorial expansion, and smoother route switching.
- Included commits:
  - `b2035ab` feat(ui): introduce calm-glass shell and layered navigation.
  - `df5636a` feat(ui): restyle core pages and add settings diagnostics.
  - `b0a8a2f` docs(ui): update v0.8.0 changelog devlog and readme notes.
  - `c66516a` fix(ui): remove duplicate language switch from sidebar.
  - `1c19ace` feat(tutorial): expand guide with full feature walkthrough.
  - `1144268` feat(ui): add playful animated task checkboxes.
  - `9b6a107` fix(ui): keep sidebar fixed and animate tab transitions.
  - `90765b7` perf(router): preload route chunks for smoother page switches.
- Release prep:
  - Moved pending entries from `Unreleased` into `CHANGELOG.md` section `## [0.8.0] - 2026-03-24`.
  - Reset `Unreleased` to placeholder state for next iteration.
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Next:
  - Push tag `v0.8.0`, monitor `publish.yml`, and finalize readable GitHub release notes.

## 2026-03-24 (Release v0.8.1)
- Date: 2026-03-24
- Goal: Publish `v0.8.1` with Android/mobile navigation IA refinement and top safe-area polish.
- Included commits:
  - `ef89d64` feat(mobile): redesign android nav with grouped tabs and more hub.
- Release prep:
  - Moved current pending changes into `CHANGELOG.md` section `## [0.8.1] - 2026-03-24`.
  - Kept `Unreleased` as placeholder for next iteration.
- Validation:
  - `npm run lint` passed.
  - `npm test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Next:
  - Push tag `v0.8.1`, monitor `publish.yml`, verify assets, and update release summary.

## 2026-03-24 (Roadmap and TODO Refresh)
- Date: 2026-03-24
- Goal: Rebuild milestones and prioritized backlog using latest product feedback (UI/UX, friendliness, and practical execution order).
- Changes:
  - Added new roadmap source doc: `docs/ROADMAP.md`.
  - Replaced outdated README roadmap blocks with:
    - concise versioned milestone sequence
    - direct link to `docs/ROADMAP.md`.
  - Updated AGENTS milestone rule to keep detailed planning in `docs/ROADMAP.md` while preserving README summary.
  - Updated README feature wording to match current mobile navigation model (`Dashboard/Record/History/More`).
- Validation:
  - Reviewed roadmap scope against current released baseline (`v0.8.1`) and in-flight fixes.
  - Confirmed milestone themes stay solo-dev practical (one theme per milestone, 3-5 outcomes).
- Risks:
  - Milestones are planning targets; exact ordering may still shift based on hotfix pressure or mobile QA findings.
- Next:
  - Execute P0 items first (`v0.8.2`/`v0.9.0`) and mirror shipped outcomes in `CHANGELOG.md`.

## 2026-03-24 (v0.8.2 Mobile Keyboard + Docs Alignment)
- Date: 2026-03-24
- Goal: Continue `v0.8.2` roadmap execution with mobile interaction polish and doc-contract alignment.
- Changes:
  - Added `useMobileKeyboardState` hook to detect software-keyboard open state on narrow/mobile layouts.
  - Updated mobile bottom navigation behavior:
    - auto-hide bottom bar while keyboard is open
    - auto-close record/history grouped sheet while keyboard is open
    - restore normal nav visibility when keyboard closes.
  - Reduced mobile content bottom padding while keyboard is open to avoid extra dead space during input.
  - Updated docs to match current behavior and priority decisions:
    - `docs/ARCHITECTURE.md` responsive-shell section now reflects `Dashboard / Record / History / More` model.
    - `docs/ROADMAP.md` clarified screenshot refresh is deferred to pre-`v1.0.0`.
    - `docs/QA_CHECKLIST.md` added mobile keyboard/nav overlay regression checks.
    - `docs/assets/screenshots/README.md` added deferred-refresh policy and mobile capture priorities.
  - Updated `CHANGELOG.md` `Unreleased` entries for ongoing `v0.8.2` work.
- Validation:
  - Pending local lint/test/build pass after this batch.
- Risks:
  - Keyboard detection is based on `visualViewport` delta heuristic; behavior may vary slightly across Android OEM WebView variants.
- Next:
  - Run lint/test/build checks and adjust thresholds if specific device models still show nav flicker while typing.

## 2026-03-24 (v0.8.2 Dashboard Readability Pass - Batch 2)
- Date: 2026-03-24
- Goal: Continue roadmap execution by improving Dashboard glanceability and first-use empty-state guidance.
- Changes:
  - Upgraded Today/This Week summary cards to show:
    - more visible completion percentage
    - remaining-item hint (`N remaining`)
    - existing progress bar retained for quick trend scanning.
  - Added Dashboard empty-state CTA actions:
    - body card links to add first body record
    - recent daily/weekly cards show fallback guidance + direct open links.
  - Added new bilingual i18n keys for remaining-item labels and Dashboard empty-state CTA copy.
  - Updated `CHANGELOG.md` `Unreleased` with this batch's user-visible behavior updates.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Dashboard cards now carry slightly denser text; may need a compact typography tweak if very small screens feel crowded.
- Next:
  - Continue `v0.9.0` foundation items: add unified operation feedback (toast layer) and Sync page summary-first information hierarchy.

## 2026-03-24 (Roadmap Detail Upgrade)
- Date: 2026-03-24
- Goal: Convert roadmap into a more detailed execution plan based on latest UI/UX and product-depth feedback.
- Changes:
  - Rewrote `docs/ROADMAP.md` into:
    - four long-term workstreams (`UI` / `UX` / `Trust` / `Depth`)
    - milestone-by-milestone outcomes from `v0.8.2` to `v1.0.0`
    - explicit acceptance focus per milestone
    - rolling backlog split (`P0/P1/P2`)
    - unchanged non-goals guardrails.
  - Added intermediate milestone themes:
    - `v0.13.0` insights/review
    - `v0.14.0` retention/routine support.
  - Updated README concise milestone sequence to match detailed roadmap structure.
- Validation:
  - Manual consistency check between `README.md` roadmap summary and `docs/ROADMAP.md`.
- Risks:
  - Detailed plan increases planning surface area; priorities may still reorder due to release hotfixes.
- Next:
  - Keep executing in small commit-sized batches and reflect each shipped slice in `CHANGELOG.md` `Unreleased`.

## 2026-03-24 (v0.9.0 Sync Summary-First Layout - Batch 1)
- Date: 2026-03-24
- Goal: Start `v0.9.0` by reducing Sync page cognitive load for non-technical users.
- Changes:
  - Reworked Sync top panel into a user-first summary block:
    - sync status badge
    - last sync timestamp
    - current mode (disabled/manual/auto-pull)
    - pending changes and conflict count
    - explicit "action needed" hint.
  - Kept sync actions visible as quick actions (`Sync Both/Push/Pull/Retry/Refresh/Export`).
  - Moved low-level runtime fields into an `Advanced diagnostics` collapsible section (auto-expanded when degraded).
  - Added new bilingual i18n keys for summary-first copy and action-hint messaging.
  - Updated `CHANGELOG.md` `Unreleased` to include this behavioral upgrade.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Some advanced users may prefer always-expanded diagnostics; currently handled by default-open when degraded.
- Next:
  - Continue `v0.9.0` with unified operation feedback (toast layer) and additional Body/Sync empty-state guidance.

## 2026-03-24 (v0.9.0 Unified Feedback - Toast Batch)
- Date: 2026-03-24
- Goal: Add a reusable operation-feedback foundation and connect Sync high-frequency actions first.
- Changes:
  - Added global toast provider + viewport (`success/error/info`) with auto-dismiss and manual dismiss.
  - Added reusable toast style tokens (`dt-toast`, success/error/info variants).
  - Integrated toast layer at app root so all pages can reuse the same feedback channel.
  - Connected Sync page actions to toasts:
    - refresh
    - sync now (push/pull/both)
    - conflict resolve (single/batch/preset)
    - diagnostics export.
  - Kept existing inline status/error text behavior for compatibility while adding toast UX for immediate feedback.
  - Updated `CHANGELOG.md` `Unreleased`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Success toast cadence may feel dense during rapid batch operations; can tune durations or merge duplicate messages in a follow-up.
- Next:
  - Extend toast usage to Body/Settings import-export and key save/update flows.

## 2026-03-24 (v0.9.0 Body Feedback + Empty State - Batch)
- Date: 2026-03-24
- Goal: Continue unified feedback and empty-state friendliness on Body page.
- Changes:
  - Integrated Body page with global toasts:
    - save success/failure
    - date required validation
    - external disk update notice.
  - Added Body history empty-state panel with clear first-record guidance when no records are visible.
  - Added new bilingual Body i18n copy for empty-state title/hint.
  - Updated `CHANGELOG.md` `Unreleased`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Frequent external file edits could generate repeated “updated from disk” toasts; may need debounce/merge logic in future.
- Next:
  - Extend toast integration to Settings import/export/migration/reset operations and key profile actions.

## 2026-03-24 (v0.10.0 Daily Efficiency - Batch 1)
- Date: 2026-03-24
- Goal: Start daily high-frequency interaction improvements with minimal schema risk.
- Changes:
  - Added row-click toggle behavior for Daily checklist items (core + optional):
    - users can click the task row to toggle completion
    - input/button interactions remain excluded to avoid accidental toggles while editing text.
  - Added Daily Core remaining-items hint (`N remaining`) under progress bar for clearer completion target.
  - Added bilingual i18n key for Daily remaining-items copy.
  - Updated `CHANGELOG.md` `Unreleased`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Row-click hit area could still feel sensitive for users who click near text input boundaries; current target filtering avoids most accidental toggles.
- Next:
  - Continue daily efficiency with stronger structured/raw mode guidance and optional keyboard shortcuts.

## 2026-03-24 (v0.10.0 Mode Guidance - Batch 2)
- Date: 2026-03-24
- Goal: Clarify structured vs raw editing intent for mixed user types.
- Changes:
  - Added mode guidance helper text in Daily and Weekly pages:
    - structured mode = recommended for fast checklist edits
    - raw markdown mode = advanced for free-form editing.
  - Added bilingual i18n keys for mode guidance.
  - Updated `CHANGELOG.md` `Unreleased`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Toolbar density increased slightly on narrow widths; kept hint wrapping behavior to avoid overlap.
- Next:
  - Continue with Daily/Weekly autosave feedback polish and optional keyboard shortcuts.

## 2026-03-24 (v0.9.0 Settings Feedback Coverage - Batch)
- Date: 2026-03-24
- Goal: Extend unified operation feedback to high-impact Settings actions.
- Changes:
  - Connected Settings explicit actions to global toasts:
    - WebDAV test/push/pull/delete snapshot
    - export bundle
    - import bundle
    - migrate data root
    - reset tracker data.
  - Kept existing inline section messages for continuity while adding global immediate feedback.
  - Updated `CHANGELOG.md` `Unreleased`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Duplicate perception (inline + toast) is intentional for now; can later tune to toast-only or context-dependent display.
- Next:
  - Decide whether to release `v0.8.2`/`v0.9.0` staging tag or continue with Daily keyboard shortcut and history filter batches.

## 2026-03-24 (Release v0.9.0)
- Date: 2026-03-24
- Goal: Publish `v0.9.0` with mobile polish, summary-first sync UX, unified toast feedback foundation, and daily-efficiency upgrades.
- Included commits:
  - `b46edd9` fix(tutorial): use mobile-specific tour steps and responsive overlay.
  - `652b5f7` feat(preferences): add profile-level typography scale control.
  - `b9755d8` docs(roadmap): refresh milestones and prioritized todo backlog.
  - `78b717c` fix(mobile): improve keyboard-safe nav behavior and align v0.8.2 docs.
  - `a1d6f70` feat(dashboard): improve summary hierarchy and empty-state guidance.
  - `21eb9ff` docs(roadmap): expand milestones into detailed execution tracks.
  - `fd21111` feat(sync): add summary-first layout with collapsible diagnostics.
  - `43f135c` feat(feedback): add global toast layer and wire sync actions.
  - `8bae6fa` feat(body): add toast feedback and first-record empty state.
  - `2b9b7e9` feat(daily): add row-toggle interaction and remaining-item hint.
  - `cc98698` feat(notes): add structured-vs-raw mode guidance hints.
  - `5e4930e` feat(settings): add toast feedback for data and webdav actions.
- Release prep:
  - Moved `CHANGELOG.md` `Unreleased` entries into `## [0.9.0] - 2026-03-24`.
  - Reset `Unreleased` section to placeholder state.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Next:
  - Push tag `v0.9.0`, monitor `publish.yml`, verify release assets, and update readable release notes.

## 2026-03-24 (v0.10.0 History Efficiency - Batch 3)
- Date: 2026-03-24
- Goal: Improve Daily/Weekly history retrieval speed and glanceable completion context.
- Changes:
  - Upgraded Daily/Weekly history pages with completion badges per row (`checked/total` + percent).
  - Added status filter controls (`All / Completed / Pending`) with visible count feedback.
  - Added fast-open flow for search results:
    - press `Enter` to open first match
    - click quick action button to open first match.
  - Added explicit empty-state hint when current query/filter has no matching notes.
  - Added bilingual i18n copy for all new history controls and states.
  - Updated `CHANGELOG.md` `Unreleased` and `docs/ROADMAP.md` progress snapshot.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Completion badges currently derive from full note parse and may add extra read cost on very large histories.
- Next:
  - Continue `v0.10.0` with autosave feedback polish and optional Daily/Weekly keyboard efficiency shortcuts.

## 2026-03-24 (v0.11.0 Body Analytics - Batch 1)
- Date: 2026-03-24
- Goal: Start body analytics milestone with fast trend-window switching and immediate change context.
- Changes:
  - Added `src/features/body/body.analytics.ts`:
    - `filterBodyRecordsByRange(...)` (`7d/30d/90d/all`)
    - `metricDeltaFromLatest(...)` (latest vs previous).
  - Added `src/features/body/body.analytics.test.ts` with 5 tests for range and delta logic.
  - Updated Body page:
    - chart-range segmented controls
    - per-metric delta badges (`vs previous`) aligned with metric unit/decimal preferences
    - selected range badge shown in each chart card title row.
  - Added bilingual i18n keys for body range/delta labels.
  - Updated `CHANGELOG.md` `Unreleased` and `docs/ROADMAP.md` v0.11 progress snapshot.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (13 files, 37 tests).
  - `npm run build` passed.
- Risks:
  - Current delta badges compare only latest vs immediately previous record; longer-window trend summaries are still pending.
- Next:
  - Continue v0.11 with optional goal line and post-submit “new point + change summary” feedback polish.

## 2026-03-24 (v0.11.0 Body Analytics - Batch 2 Goal Lines)
- Date: 2026-03-24
- Goal: Add configurable goal-line support for body metrics with local preference persistence.
- Changes:
  - Preferences schema upgraded to `v5` with `body.goals.<metric> = { enabled, value }`.
  - Added body goal normalization/default helpers and upgraded tests for new fields.
  - Extended Preferences UI:
    - per-metric goal-line toggle
    - per-metric numeric goal value input.
  - Extended Body trend charts with per-metric `ReferenceLine` when goal-line is enabled and value is set.
  - Updated docs:
    - `docs/PREFERENCES_SCHEMA.md`
    - `docs/DATA_MODEL.md`
    - `docs/ROADMAP.md`
    - `README.md`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (13 files, 37 tests).
  - `npm run build` passed.
- Risks:
  - Goal values currently rely on numeric input normalization and do not enforce metric-specific min/max ranges.
- Next:
  - Continue v0.11 with post-submit highlight and concise change summary feedback on Body page.

## 2026-03-24 (v0.11.0 Body Analytics - Batch 3 Save Feedback)
- Date: 2026-03-24
- Goal: Make body record submission feedback more actionable and visible in-place.
- Changes:
  - Added post-submit summary feedback comparing current record with previous record:
    - detailed delta summary when numeric changes exist
    - no-delta / no-previous fallback messages.
  - Added temporary highlight behavior after submit:
    - highlighted chart point for the saved record
    - highlighted history row/card for the saved record.
  - Added i18n keys for the new feedback copy.
  - Updated roadmap/changelog/readme progress notes.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (13 files, 37 tests).
  - `npm run build` passed.
- Risks:
  - Summary currently compares to the immediately previous record in descending date order; same-day duplicates may reduce semantic clarity.
- Next:
  - Move to `v0.12.0` template/goal loop depth after a quick manual QA sweep on Body submit flows (desktop + Android).

## 2026-03-24 (v0.12.0 Templates - Batch 1 Safer Apply UX)
- Date: 2026-03-24
- Goal: Make template-update impact and overwrite risk more explicit before apply.
- Changes:
  - Profiles template-update panel now shows clearer impact preview copy:
    - file impact count
    - apply mode
    - existing daily/weekly/reflection diff summary.
  - Added overwrite-specific risk banner when preview shows removed items.
  - Added explicit overwrite acknowledgment checkbox gate before apply action.
  - Added apply guard message if overwrite acknowledgment is missing.
  - Updated roadmap/changelog/readme notes for v0.12 progress.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (13 files, 37 tests).
  - `npm run build` passed.
- Risks:
  - Acknowledgment gating currently applies to all overwrite attempts, even when preview delta is small.
- Next:
  - Continue v0.12 with lightweight weekly goals/progress indicators.

## 2026-03-24 (v0.12.0 Templates/Goals - Batch 2 Weekly Progress Panel)
- Date: 2026-03-24
- Goal: Add lightweight weekly goal/progress indicators without introducing a new storage schema.
- Changes:
  - Weekly structured page now includes a top goal-progress panel:
    - completed-sections summary (`met/total`)
    - weakest-section hint when incomplete
    - per-section completion badges.
  - Reused existing checklist data (local markdown) for goal computation; no hidden storage introduced.
  - Added bilingual i18n copy and updated roadmap/changelog/readme notes.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (13 files, 37 tests).
  - `npm run build` passed.
- Risks:
  - Current “goal met” rule is strict (`checked === total`) and not yet configurable per section.
- Next:
  - Continue v0.12 deeper template customization semantics (category/order/required) in small, schema-safe slices.

## 2026-03-24 (Release v0.10.0)
- Date: 2026-03-24
- Goal: Ship history efficiency improvements as a standalone release.
- Changes:
  - Selected release commit scope ending at `533421d` (Daily/Weekly history retrieval improvements).
  - Updated changelog with `## [0.10.0] - 2026-03-24`.
  - Created and pushed `v0.10.0` tag for publish workflow.
- Validation:
  - Release workflow and asset checks executed after tag push.
- Risks:
  - This release intentionally excludes subsequent body analytics upgrades.
- Next:
  - Publish `v0.11.0` with body analytics milestone scope.

## 2026-03-24 (Release v0.11.0)
- Date: 2026-03-24
- Goal: Ship body analytics milestone as separate follow-up release.
- Changes:
  - Selected release commit scope ending at `b778015` (chart ranges, delta chips, goal lines, save feedback/highlights).
  - Updated changelog with `## [0.11.0] - 2026-03-24`.
  - Created and pushed `v0.11.0` tag for publish workflow.
- Validation:
  - Release workflow and artifact checks executed after tag push.
- Risks:
  - This release intentionally excludes `v0.12` template/weekly-goal UX iterations.
- Next:
  - Keep `Unreleased` focused on v0.12+ work and continue roadmap batching.

## 2026-03-24 (v0.12.0 Templates - Batch 3 Reorder + Locale Consistency)
- Date: 2026-03-24
- Goal: Improve template customization ergonomics and remove mixed-language labels in English UI.
- Changes:
  - Added structured template checklist reorder controls (`Move Up` / `Move Down`) in Profiles page for:
    - create-profile template editor (daily + weekly)
    - current-profile template editor (daily + weekly).
  - Added reusable reorder helper `reorderByOffset` with unit tests.
  - Updated i18n copy:
    - added `profiles.moveUp` / `profiles.moveDown` (en/zh)
    - switched English locale language labels to `English / Chinese` for onboarding/template selectors.
  - Updated release skill docs to enforce English-by-default release notes.
  - Updated roadmap/changelog progress notes.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Reorder controls currently move one step per click; drag-and-drop ordering remains future work.
- Next:
  - Continue v0.12 template-depth slices (required/category semantics) while keeping markdown compatibility.

## 2026-03-25 (v0.12.0 Templates - Batch 4 Required/Category Semantics)
- Date: 2026-03-25
- Goal: Deliver the next practical slice of deeper template customization while staying markdown-compatible.
- Changes:
  - Profiles structured template editor now supports Daily required semantics by moving items between:
    - `Daily Core` (required)
    - `Optional` (optional).
  - Profiles structured template editor now supports Weekly category semantics by changing per-item section bucket:
    - `Body / Research / Life / Output / Social`.
  - Added reusable list-move helper and tests:
    - `src/features/settings/templateMove.ts`
    - `src/features/settings/templateMove.test.ts`.
  - Kept reordering controls and combined them with category/required controls for both create-profile and current-profile template editors.
  - Updated i18n copy (en/zh), roadmap, changelog, parser spec, and README.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - Category reassignment currently appends moved items to target section tail; drag-and-drop cross-section placement is not implemented yet.
- Next:
  - Continue v0.12 with richer template semantics (for example stronger required rules and optional rule metadata) without breaking markdown roundtrip behavior.

## 2026-03-25 (Android Path Alias Fix + Error Diagnostics)
- Date: 2026-03-25
- Goal: Fix Android initialization/new-day/diagnostics export regressions and expose actionable error details.
- Changes:
  - Fixed Rust writable-path validation for non-existing targets under symlink/alias roots.
    - Root cause: relative target was computed against canonical ancestor path, which failed when raw path and canonical path used different Android aliases (`/data/data` vs `/data/user/0`).
    - Updated validation to compute relative path from the original existing ancestor path, then join onto canonical ancestor.
  - Added shared frontend error extractor (`src/lib/error.ts`) and applied it to key flows:
    - onboarding template apply
    - data-root bootstrap error surface
    - dashboard load failure message
    - daily note load failure message
    - sync diagnostics export failure message.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - This fix targets path-alias mismatch; if a device has true filesystem permission constraints outside app sandbox, separate handling is still required.
- Next:
  - Re-verify Android first-run template apply, day-rollover auto-create, and diagnostics export on physical device.

## 2026-03-25 (Android Onboarding Template Language Mismatch Fix)
- Date: 2026-03-25
- Goal: Fix first-run onboarding where selecting Chinese template could still result in English Today/This Week content.
- Changes:
  - App shell now blocks route outlet rendering while initial template setup is active.
    - Prevents background page effects (Dashboard Today/Week load) from auto-creating notes with default template before onboarding apply.
  - `completeInitialTemplateSetup` now force-applies selected template content to:
    - `daily/<today>.md`
    - `weekly/<current-week>.md`
    immediately after saving selected templates.
  - Updated changelog notes for this onboarding consistency fix.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Onboarding apply currently overwrites today/current-week files by design for first-run consistency.
- Next:
  - Rebuild Android artifact and verify first-run zh template selection yields zh Today/This Week immediately.

## 2026-03-25 (Onboarding New/Existing Split + In-Modal Existing Flow)
- Date: 2026-03-25
- Goal: Reduce first-run friction by finishing existing-user setup inside onboarding instead of requiring Settings detour.
- Changes:
  - Initial onboarding flow now uses four steps:
    - `language` -> `user type` -> (`template` for new users | `existing` for existing users).
  - Added existing-user onboarding mode with in-modal actions:
    - local export-folder import (`importDataBundle`) and continue
    - WebDAV config/test/pull latest snapshot and continue
    - skip for now (finish onboarding directly).
  - Added `finishInitialTemplateSetup(...)` in `DataRootContext` so onboarding can close without template apply for existing-user paths.
  - Added bilingual onboarding copy for user-type selection and existing-user import/WebDAV actions.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Existing-user import path currently uses manual source-path input; native folder picker can further reduce typo risk.
- Next:
  - Verify Android first-run flow with both existing-user paths (import and WebDAV pull) on physical devices.

## 2026-03-25 (Path Picker for Onboarding/Settings)
- Date: 2026-03-25
- Goal: Reduce manual path typing friction (especially on Android) by adding native folder pickers.
- Changes:
  - Integrated Tauri dialog plugin in both frontend and backend runtime:
    - JS: `@tauri-apps/plugin-dialog`
    - Rust: `tauri-plugin-dialog`
    - capability: `dialog:default`.
  - Added shared frontend helper:
    - `src/lib/fs/dialogApi.ts` with `pickDirectory(...)`.
  - Hooked folder picker into onboarding and settings:
    - onboarding existing-user import source
    - settings migration target
    - settings export destination
    - settings import source.
  - Added bilingual i18n copy for `Browse` action and picker failure feedback.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Platform-specific directory-picker behavior may still vary by Android ROM/file-provider implementation.
- Next:
  - Validate physical Android UX for import/export path selection and confirm no regression in manual path fallback.

## 2026-03-25 (Android Folder Picker Compatibility Fallback)
- Date: 2026-03-25
- Goal: Fix Android runtime error `folder picker is not implemented on mobile` after introducing path picker buttons.
- Changes:
  - Updated `pickDirectory(...)` fallback strategy in `src/lib/fs/dialogApi.ts`:
    - try native directory picker first
    - if mobile reports directory picker unsupported, switch to file picker
    - derive parent directory from picked file path automatically.
  - Kept existing desktop behavior unchanged.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Mobile fallback relies on selecting an existing file inside the target folder (cannot directly select an empty folder).
- Next:
  - Rebuild Android artifact and verify onboarding/settings path pickers on physical devices.

## 2026-03-25 (Android Import via File Picker + Zip Support)
- Date: 2026-03-25
- Goal: Make Android old-user onboarding import practically usable despite mobile folder-picker limitation.
- Changes:
  - Added frontend smart import API:
    - `importDataBundleSmart(...)` supports directory import and zip-bytes import paths.
    - Content/file URI or `.zip` path is treated as zip source and read via `@tauri-apps/plugin-fs`.
  - Added backend command:
    - `import_data_bundle_zip(zip_bytes, data_root, overwrite)`:
      - safely extracts zip into temp directory using enclosed paths
      - validates expected bundle layout (`daily/`, `weekly/`, `body.csv`)
      - reuses existing import copy behavior.
  - Registered Tauri FS plugin and capability permission:
    - Rust plugin `tauri-plugin-fs`
    - capability `fs:default`.
  - Updated onboarding/settings import browse behavior:
    - desktop: directory picker
    - mobile unsupported-directory case: fallback to file picker and smart import path.
  - Updated i18n labels to clarify import source can be folder or `.zip`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Mobile fallback is best with zipped export bundles; importing arbitrary picked files will fail fast with validation errors.
- Next:
  - Build Android artifact and verify onboarding old-user import with a real exported zip file.

## 2026-03-25 (Android Fixed-Root UX + Zip Export + Share/Save Handoff)
- Date: 2026-03-25
- Goal: Align Android behavior with mobile constraints and simplify transfer flows:
  - fixed local root on mobile
  - zip-only portable data exchange
  - post-export share/save actions.
- Changes:
  - Export pipeline switched to zip-first in Rust:
    - `export_data_bundle` now writes `dailytrack-export-<timestamp>.zip`.
  - Settings mobile behavior adjusted:
    - Android migration disabled in UI (fixed data root model).
    - Android export destination fixed to `<base-root>/exports`.
    - Android import path picker uses file picker (`.zip`-first), desktop keeps folder/zip options.
  - Added Android export handoff actions after successful export:
    - `Share zip` via system share sheet when available.
    - `Save zip as...` via save dialog to user-chosen target path.
  - Updated onboarding import guidance for Android zip-first workflow.
  - Updated i18n copy (en/zh), changelog, and README notes.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - System share capability may vary by Android WebView/device policy; unsupported devices show explicit fallback message.
- Next:
  - Verify on physical Android device:
    - export zip -> share to IM/cloud app
    - export zip -> save-as to user-selected location
    - onboarding old-user import from selected zip.

## 2026-03-25 (Publish Pipeline Android Parallelization)
- Date: 2026-03-25
- Goal: Reduce end-to-end release time by allowing Android packaging to run in parallel with desktop packaging.
- Changes:
  - Updated GitHub Actions release workflow:
    - `publish-android-apk` now depends only on `verify-frontend`.
    - Removed unnecessary dependency on `publish-tauri`.
  - Kept artifact contract unchanged:
    - Android job still consumes `web-dist` from `verify-frontend`.
- Validation:
  - Reviewed workflow dependency graph in `.github/workflows/publish.yml` to confirm Android and Tauri jobs can run concurrently after frontend verification.
- Risks:
  - If both jobs upload to the same release at the same time, GitHub release upload ordering is nondeterministic, but file-level `--clobber` behavior remains unchanged.
- Next:
  - Trigger a tag or `workflow_dispatch` publish run and measure the total runtime delta versus previous serial dependency behavior.

## 2026-03-25 (v0.12.0 Wrap-up and Roadmap Closure)
- Date: 2026-03-25
- Goal: Close the current v0.12.0 batch with consistent status across code, docs, and validation outputs.
- Changes:
  - Completed end-to-end validation for current local changes:
    - lint, tests, frontend build, and Tauri `cargo check`.
  - Updated roadmap state in `docs/ROADMAP.md`:
    - marked `v0.12.0` personalization baseline as done
    - removed stale `In progress` markers on earlier shipped milestones
    - moved richer rule-engine style template semantics to post-`v0.12` backlog.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Android system share/save behavior still depends on vendor ROM support and file-provider compatibility.
- Next:
  - Tag and release after final on-device Android sanity check for zip share/save/import handoff.

## 2026-03-25 (Node24 CI Compatibility + Startup Smoothness First Pass)
- Date: 2026-03-25
- Goal:
  - remove Node 20 deprecation risk in CI
  - reduce perceived UI stalls at startup by moving non-critical work further into background.
- Changes:
  - Upgraded workflow action majors for Node 24 compatibility:
    - `actions/checkout@v6`
    - `actions/setup-node@v6`
    - `actions/setup-java@v5`
    - `actions/cache@v5`
    - `actions/upload-artifact@v7`
    - `actions/download-artifact@v8`
    - `android-actions/setup-android@v4`
  - Added workflow-level `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` in:
    - `.github/workflows/publish.yml`
    - `.github/workflows/android-apk.yml`
  - Startup smoothness first pass:
    - desktop route preloading now starts later (`1200ms`) and runs incrementally with spacing
    - mobile route preloading is skipped
    - updater auto-check is delayed (`4000ms`) to avoid competing with first interaction.
  - Updated roadmap P0 to explicitly prioritize startup/resume smoothness and non-blocking background tasks.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed.
  - `npm run build` passed.
- Risks:
  - New action major versions may have subtle behavior differences in future CI runs; next publish should be monitored once for regression.
  - Incremental preload trades some first-time page-switch speed for smoother initial responsiveness.
- Next:
  - Instrument startup/resume timings (desktop + Android) and target worst offenders in follow-up optimization batch.

## 2026-03-25 (Smoothness Batch 2: Visibility-Aware Refresh + Event Coalescing)
- Date: 2026-03-25
- Goal:
  - reduce occasional desktop/Android micro-stutters on startup and app resume
  - keep user input/rendering responsive by lowering concurrent background read/sync pressure.
- Changes:
  - Dashboard:
    - added coalesced reload scheduling (debounced burst handling).
    - prevented overlapping `loadDashboard` runs and added pending-reload handoff.
    - visibility/focus reloads now use short delayed scheduling instead of immediate heavy reads.
  - Daily/Weekly list pages:
    - added incremental summary hydration cap (`slice(0, 12)`) to avoid parsing all missing history at once.
    - switched event handling to smarter updates:
      - daily/weekly content updates refresh target summary only when possible
      - full list reload reserved for profile/all-level changes.
    - added visibility-aware polling.
  - Daily/Weekly note pages:
    - polling now skips when app is hidden.
    - added resume grace window and in-flight poll guard to avoid concurrent disk reads right after foreground restore.
  - Body page:
    - polling now skips in hidden state.
  - WebDAV bridges/status:
    - delayed initial/re-foreground realtime sync scheduling in `WebdavSyncBridge`.
    - reduced sync status countdown update frequency and made status refresh visibility-aware in `useSyncStatus`.
  - Mobile typing smoothness:
    - `useMobileKeyboardState` now batches viewport resize/scroll reactions with `requestAnimationFrame` and updates state only on actual value change.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Some background status fields (for example “next auto pull in N seconds”) update less frequently than before by design.
  - Very high-frequency external file edits may appear with slight delay due reload coalescing.
- Next:
  - Add lightweight perf telemetry hooks (startup mark, resume mark, first-input delay proxy) to track optimization impact over next releases.

## 2026-03-25 (Smoothness Batch 3: Deferred Non-Critical Runtime Work)
- Date: 2026-03-25
- Goal:
  - further protect first interaction smoothness by deferring non-essential runtime work during startup.
- Changes:
  - App-level startup tuning:
    - route preload now prefers idle scheduling with delayed fallback on desktop.
    - non-critical bridges (`FilesystemWatchBridge`, `WebdavSyncBridge`) mount after delayed startup window.
  - Shell-level startup tuning:
    - `MobileSyncBanner` and `TutorialGuide` now mount after deferred window to reduce immediate reactive/event pressure.
  - Updater startup tuning:
    - updater capability probing (`getVersion`, `isUpdaterSupported`, `isUpdaterConfigured`) delayed into background window.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Some non-critical UI elements and updater readiness state appear slightly later by design.
  - If users open Settings immediately after launch, updater support/config labels may still be resolving.
- Next:
  - Add lightweight runtime perf snapshot export and use it to drive remaining Android resume latency fixes.

## 2026-03-25 (Smoothness Batch 4: Runtime Telemetry + Export)
- Date: 2026-03-25
- Goal:
  - move from “subjective smoothness” to measurable runtime signals
  - make perf diagnostics exportable for cross-device issue triage.
- Changes:
  - Added `src/lib/perf/runtimePerf.ts`:
    - startup/runtime marks
    - resume-to-first-frame series
    - route-switch-to-frame series
    - first-input delay snapshot (when supported)
    - first/large contentful paint snapshot (when supported)
    - long-task capture and summary (when supported).
  - Instrumented runtime flow:
    - `App.tsx`: perf init + startup marks + bridge-ready timing
    - `AppShell.tsx`: shell first-frame + route-switch timing
    - `DashboardPage.tsx`, `DailyNotePage.tsx`, `WeeklyNotePage.tsx`: load duration sampling.
  - Added Settings diagnostics export action:
    - new button in diagnostics panel
    - exports `perf-diagnostics/perf-diagnostic-<timestamp>.json` into local data root.
  - Added i18n copy (en/zh) for performance diagnostics actions and export feedback.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Some browser/webview perf APIs are capability-dependent; unsupported metrics are exported as `null`/empty.
  - Telemetry is intentionally lightweight and not persisted continuously (snapshot-oriented).
- Next:
  - Add a compact in-app “recent perf summary” card (optional) and correlate spikes with sync/poll events on Android resume.

## 2026-03-25 (Hotfix: Perf Diagnostics Export Path Validation)
- Date: 2026-03-25
- Goal: fix runtime performance diagnostics export failure in Settings.
- Changes:
  - Updated Settings perf diagnostics export to write into current profile root:
    - from base-root path to `<profile-root>/perf-diagnostics/perf-diagnostic-<timestamp>.json`
  - Kept payload metadata unchanged (`baseDataRoot` + `profileRoot` are still included for diagnosis context).
- Validation:
  - `npm run lint` passed.
  - `npm run build` passed.
- Risks:
  - Export files are now profile-scoped (not base-root scoped), which is consistent with current filesystem write-guard policy.
- Next:
  - Include this fix in the next patch release so users no longer hit Settings perf export failures.

## 2026-03-25 (UX Polish: Route Loading Transition)
- Date: 2026-03-25
- Goal: reduce jarring perception during tab/page switches when route chunks are still loading.
- Changes:
  - Updated route fallback in `src/app/router.tsx`:
    - introduced a short delayed-visibility threshold (~140ms) before showing fallback UI.
    - replaced plain loading text with a structured fallback skeleton container.
  - Added route-loading visual tokens and animations in `src/styles/tokens.css`:
    - skeleton shimmer lines + subtle fade-in animation
    - reduced-motion handling (`prefers-reduced-motion`) to disable motion where appropriate.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - On very slow devices, users still see loading fallback, but now with smoother visual transition.
- Next:
  - Evaluate whether route-level warm caching can further reduce visible fallback frequency on Android resume.

## 2026-03-25 (Smoothness Batch 5: Settings Route Spike Reduction)
- Date: 2026-03-25
- Goal: reduce residual micro-stutter when navigating into Settings (observed as `/settings` route-switch spikes in perf diagnostics).
- Changes:
  - Updated `SettingsPage` WebDAV block to be collapsed by default:
    - added explicit expand/collapse action
    - render heavy WebDAV form/snapshot DOM only after section expand.
  - Switched WebDAV bootstrap strategy to lazy-on-expand:
    - defer initial `getWebdavConfig + listWebdavSnapshots` until first expand
    - guard autosave flow until bootstrap completes.
  - Adjusted route background preload priority so Settings-related chunks are warmed earlier.
  - Added i18n copy (en/zh) for section expand/collapse and lazy-loading hints.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - First expand of WebDAV section now intentionally incurs a small one-time load delay.
- Next:
  - Re-capture perf diagnostics after several Settings navigations to verify `/settings` p95 route-switch latency improvement.

## 2026-03-25 (Smoothness Batch 6: Update Notification Jank Reduction)
- Date: 2026-03-25
- Goal: reduce perceived stutter when update-available notification appears.
- Changes:
  - Updater scheduling:
    - auto-check is now dispatched during idle window after startup delay.
    - updater result state updates are wrapped in `startTransition` to reduce urgent render pressure.
  - App shell update notification:
    - replaced in-flow update banner with floating overlay banner to avoid pushing main content layout.
    - added lightweight entry animation with reduced-motion fallback.
  - Added/updated related styling tokens in `src/styles/tokens.css`.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (15 files, 43 tests).
  - `npm run build` passed.
- Risks:
  - Floating banner can overlap top-right content on very small viewports, though current offset keeps primary controls usable.
- Next:
  - Add explicit perf series marks for updater check-start/check-complete/banner-shown to directly correlate future diagnostics with update flow.

## 2026-03-25 (v0.13.0 Kickoff: Dashboard Insights + Structured Review Snapshot)
- Date: 2026-03-25
- Goal: start `v0.13.0` implementation with actionable insight signals on Dashboard and export-friendly review structure in reports.
- Changes:
  - Dashboard insight baseline in `src/pages/DashboardPage.tsx`:
    - added period-over-period comparison (`today` / `this week` vs previous tracked period)
    - added daily and weekly completion streak indicators
    - added weakest-section hint and local rule-based `Next Best Action` card.
  - Added dashboard insight helpers in `src/features/dashboard/dashboard.service.ts`:
    - progress comparison utility (`up/down/flat/na`)
    - consecutive day/week detection helpers
    - weakest-section selector.
  - Added unit tests in `src/features/dashboard/dashboard.service.test.ts` covering comparison, continuity, and weakest-section logic.
  - Enhanced report output structure in `src/features/reports/reports.service.ts`:
    - generated weekly/monthly markdown now includes deterministic `## Structured Snapshot` before narrative output.
  - Updated docs and release tracking (`README.md`, `docs/ROADMAP.md`, `CHANGELOG.md`).
- Validation:
  - `npm run test` passed (16 files, 49 tests).
  - `npm run build` passed.
- Risks:
  - Dashboard streak calculation is intentionally based on consecutive completed tracked periods and may stop at first date/week gap.
  - Report `Structured Snapshot` is deterministic but currently not covered by dedicated snapshot tests.
- Next:
  - Add export schema tests for report structured snapshot consistency.
  - Continue `v0.13.0` with richer report insights and monthly review depth.

## 2026-03-25 (v0.13.0 Follow-up: Report Local Recommendations + Tests)
- Date: 2026-03-25
- Goal: make report output more export-friendly and deterministic by adding model-independent recommendation baseline.
- Changes:
  - Extended `src/features/reports/reports.service.ts` with:
    - structured snapshot stats builder
    - local rule-based recommendation builder
    - report markdown now includes `## Local Recommendations` section.
  - Added `src/features/reports/reports.service.test.ts` for snapshot stats and recommendation rules.
  - Synced docs/changelog/roadmap wording for this follow-up.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
- Risks:
  - Recommendation copy is currently English-only content embedded in report markdown body.
- Next:
  - Optional: add locale-aware recommendation copy and recommendation rule customization by profile preferences.

## 2026-03-25 (v0.13.0 Follow-up: Report Period Comparison + Language-Aware Output)
- Date: 2026-03-25
- Goal: make report output more review-usable by adding previous-period comparison and language-aware markdown sections.
- Changes:
  - Updated `src/features/reports/reports.types.ts` and `src/pages/ReportsPage.tsx`:
    - report build input now carries current UI language (`en` / `zh`).
  - Refactored `src/features/reports/reports.service.ts`:
    - added reusable period-data loader for current/previous target selection,
    - added structured snapshot comparison lines vs previous period,
    - added localized report sections (`Structured Snapshot` / `Local Recommendations` / `Narrative Review`) for Chinese/English output,
    - localized local rule-based recommendation copy.
  - Expanded `src/features/reports/reports.service.test.ts` with comparison-decline and zh-language recommendation coverage.
  - Updated roadmap/changelog/readme wording for this delta.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (17 files, 54 tests).
  - `npm run build` passed.
- Risks:
  - Existing already-generated report files remain in previous format until regenerated.
- Next:
  - Optional: expose previous-target override in Reports UI for manual comparison baseline selection.

## 2026-03-25 (v0.13.0 Follow-up: Report Companion JSON Export)
- Date: 2026-03-25
- Goal: finish export-friendly report output by adding machine-readable companion file for downstream parsing.
- Changes:
  - Extended report output contract in `src/features/reports/reports.types.ts` with optional `reportJsonPath`.
  - Updated `src/features/reports/reports.service.ts`:
    - report generation now writes both markdown and JSON files,
    - added `buildReportExportPayload` (`schemaVersion=1`) with snapshot/comparison/recommendation fields.
  - Updated `src/pages/ReportsPage.tsx` message to show markdown + JSON output paths.
  - Added i18n message key for dual-path report generation feedback.
  - Expanded `src/features/reports/reports.service.test.ts` with export payload coverage.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed (17 files, 55 tests).
  - `npm run build` passed.
- Risks:
  - Existing historical reports do not have companion JSON unless regenerated.
- Next:
  - Optional: add a small “Export Schema” help block in Reports page to document JSON fields for external tooling.

## 2026-03-25 (Release: v0.13.0)
- Date: 2026-03-25
- Goal: cut `v0.13.0` release from completed roadmap scope and publish tag.
- Changes:
  - Verified release baseline from `v0.12.2..HEAD` includes two feature commits for dashboard insights and report guidance/export improvements.
  - Ran release quality gates (`lint`, `test`, `build`, `cargo check`).
  - Moved changelog entries from `Unreleased` into `## [0.13.0] - 2026-03-25` and reset `Unreleased` placeholder.
  - Prepared tag/release workflow execution via `dailytrack-release` runbook.
- Validation:
  - `npm run lint` passed.
  - `npm run test` passed.
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - GitHub publish workflow and release-asset verification depend on remote CI completion after tag push.
- Next:
  - Push `v0.13.0` tag, monitor publish workflow, verify release assets, and finalize human-readable release notes.

## 2026-03-25 (v0.14.0 routine support baseline)
- Date: 2026-03-25
- Goal: Ship v0.14.0 roadmap baseline for retention and routine support.
- Changes:
  - Added configurable reminder strategy to profile preferences (`dailyGapDays`, `weeklyGapWeeks`, `bodyGapDays`, enable toggle) with schema upgrade and normalization tests.
  - Added dashboard reminder card and 28-day daily completion heatmap.
  - Added lightweight `Mood & Energy` tags to daily note schema, parser/serializer, structured editor, and template diff logic.
  - Expanded Daily/Weekly history filters with recency and content-aware filters; keyword search now matches one-line/mood-energy/reflection previews.
  - Updated daily template files to include `## Mood & Energy` section and synchronized roadmap/changelog/readme docs.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
- Risks:
  - Dashboard reminder strategy is currently based on latest completed checklist signals; users expecting "file edited" based reminders may need a future toggle.
  - Heatmap currently uses a fixed 28-day window and does not yet support custom range controls.
- Next:
  - Start v0.14.0 follow-up polish: weekly review integration for mood/energy trends and optional filter chips on dashboard/list pages.

## 2026-03-25 (v0.14.0 Follow-up: Daily/Weekly Calendar Views)
- Date: 2026-03-25
- Goal: add calendar-style history navigation so review flow is less list-heavy.
- Changes:
  - Added Daily Notes monthly calendar view:
    - month navigation
    - per-day completion color state
    - click day to open/create corresponding daily note.
  - Added Weekly Notes yearly ISO week grid:
    - year navigation
    - per-week completion color state
    - click week cell to open/create corresponding weekly note.
  - Added EN/ZH i18n keys for calendar labels/tooltips and updated changelog/roadmap/readme wording.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
- Risks:
  - Daily calendar currently uses Sunday as first column and fixed monthly granularity.
  - Weekly grid currently renders one full ISO year per view without quarter grouping.
- Next:
  - Optional: add "today/current-week" quick jump and calendar range presets.

## 2026-03-25 (v0.14.0 Fix: Existing-User Onboarding WebDAV Conflict Guard)
- Date: 2026-03-25
- Goal: prevent old-user onboarding from creating avoidable WebDAV conflicts before manual pull/import.
- Changes:
  - Fixed onboarding WebDAV pull target root in `InitialTemplateSetupModal`:
    - now uses base data root (same scope as Settings WebDAV pull) instead of active profile root.
  - Added onboarding-time guard in `WebdavSyncBridge`:
    - skip realtime auto sync while `needsInitialTemplateSetup` is true.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
- Risks:
  - If user intentionally wants realtime WebDAV before finishing onboarding, bridge now waits until onboarding ends by design.
- Next:
  - Optional: add explicit onboarding UI hint that realtime sync starts only after setup completion.

## 2026-03-25 (v0.14.0 Follow-up: Onboarding WebDAV Sync Hint)
- Date: 2026-03-25
- Goal: make onboarding WebDAV behavior explicit to reduce conflict-related confusion for existing users.
- Changes:
  - Added onboarding i18n copy (`en/zh`) clarifying realtime WebDAV sync starts after onboarding completion.
  - Rendered the hint in Existing User -> WebDAV onboarding panel.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
- Risks:
  - Hint reduces confusion but does not replace deeper diagnostics when remote state is already divergent.
- Next:
  - Optional: add one-tap "check conflict status" CTA after onboarding completes.

## 2026-03-25 (v0.14.0 Follow-up: Calendar Quick Jump Controls)
- Date: 2026-03-25
- Goal: speed up daily/weekly history navigation from new calendar views.
- Changes:
  - Daily monthly calendar now supports:
    - quick jump to current month
    - one-click open today
    - visual highlight for today's cell.
  - Weekly year grid now supports:
    - quick jump to current year
    - one-click open current week
    - visual highlight for current-week cell.
  - Added matching EN/ZH i18n copy and updated changelog wording.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
- Risks:
  - Current-period highlight style is intentionally strong and may need later theming tweaks.
- Next:
  - Optional: add keyboard shortcuts for jump/open actions.

## 2026-03-25 (Release: v0.14.0)
- Date: 2026-03-25
- Goal: publish `v0.14.0` with verified CI artifacts and human-readable release notes.
- Changes:
  - Ran full release quality gates:
    - `npm run lint`
    - `npm run test`
    - `npm run build`
    - `cargo check --manifest-path src-tauri/Cargo.toml`
  - Cut changelog into `## [0.14.0] - 2026-03-25` and reset `Unreleased`.
  - Created and pushed tag `v0.14.0`.
  - Verified GitHub Actions publish run `23543307922` completed successfully.
  - Verified release assets include Windows/macOS/Android artifacts, updater signatures, and `latest.json`.
  - Replaced auto-generated release text with English human-readable notes via `gh release edit`.
- Validation:
  - `gh run view 23543307922` shows all publish jobs succeeded.
  - `gh release view v0.14.0 --json assets` confirms expected asset set and updater metadata.
- Risks:
  - macOS artifacts remain unsigned/notarization-incomplete by current CI secret setup policy.
- Next:
  - Start next milestone iteration and keep `Unreleased` scoped to post-`v0.14.0` work only.

## 2026-03-25 (Codebase Review + Quick Fixes + Input Lag Fix)
- Date: 2026-03-25
- Goal: Full codebase review, apply quick code-quality wins, and fix input lag in note editors.
- Changes:
  - Completed full-repo review (19k LOC frontend, 3.2k LOC Rust, 1.1k LOC tests) and documented findings.
  - Quick fixes:
    - Added `ErrorBoundary` component wrapping the entire app for graceful crash recovery.
    - Extracted hardcoded default templates to `src-tauri/src/templates/{daily,weekly}_default.md` via `include_str!`.
    - Made `generate_llm_report` async (no longer blocks Tauri thread during LLM calls).
    - Filled in `Cargo.toml` metadata (name, version, license, repository).
    - Removed `.DS_Store` from git tracking.
  - Input lag fix in `DailyNotePage` and `WeeklyNotePage`:
    - Removed per-keystroke `serializeDailyMarkdown`/`serializeWeeklyMarkdown` useMemo that ran full re-serialization on every character typed just for dirty checking.
    - Replaced effect-driven autosave debounce with imperative `scheduleAutosave()` using stable refs — eliminates effect cleanup/setup churn per keystroke.
    - Stabilized `performSave` callback by reading `note`/`rawDraft`/`saving` from refs instead of closure deps.
    - Stabilized poll `useEffect` by checking dirty/saving state from refs inside the interval callback instead of as effect deps — poll interval no longer torn down and recreated per keystroke.
  - Docs:
    - Added post-v1.0 roadmap milestones (v1.1.0 search/nav, v1.2.0 data safety, v1.3.0 analytics, v2.0.0 extensibility).
    - Added workstream 5 (Code Quality and Reliability) to roadmap.
    - Updated README milestone sequence.
    - Updated CHANGELOG `Unreleased` section.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` (tsc) passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
- Risks:
  - Autosave debounce is now imperative (timer refs) instead of declarative (useEffect deps). Behavior is equivalent but code style is different from the rest of the codebase.
  - `serializeDailyMarkdown` import was removed from DailyNotePage; if future code needs render-time serialization there, the import must be re-added.
- Next:
  - Consider applying the same imperative autosave pattern to BodyPage if similar lag is reported there.
  - Begin v1.0.0 reliability work (recovery guardrails, quality gates, troubleshooting docs).

## 2026-03-26 (UI-First Performance Refactor)
- Date: 2026-03-26
- Goal: Eliminate re-render cascades and background work contention that cause micro-stutters, especially on Android.
- Changes:
  - Fix 1: Split `ToastContext` into `ToastDispatchContext` (stable callbacks) and `ToastStateContext` (toast array). Pages that only push toasts no longer re-render when toasts change. Only `ToastViewport` subscribes to state.
  - Fix 2: Split `UpdaterContext` into `UpdaterBannerContext` (5 fields for AppShell) and `UpdaterFullContext` (full state for Settings). AppShell no longer re-renders on updater checking/status/error/downloadPercent changes.
  - Fix 3: Dashboard parallel batch loading — replaced sequential `for-await` loops (up to 45 daily + 24 weekly notes) with `Promise.all` batches of 8. Significantly reduces wall-clock dashboard load time on Android.
  - Fix 4: Stabilized BodyPage poll effect — moved `pushInfo` and `t` to refs so poll interval is not torn down and recreated whenever any toast fires.
  - Fix 5: Made `useSyncStatus` lazy — added `enabled` parameter; `MobileSyncBanner` passes `enabled` based on mobile viewport + preference. Eliminates 10s poll + 5s countdown tick on desktop and when banner is disabled.
  - Fix 6: Replaced `useMobileKeyboardState` per-component `useState`/`useEffect` with a module-level singleton using `useSyncExternalStore`. Both AppShell and BottomNav now share one set of 4 event listeners instead of two separate sets.
  - Fix 7: Wrapped `BottomNav` in `React.memo` and extracted `pathname` to avoid full location object triggering re-renders.
  - Fixed lint errors: moved render-time ref assignments to `useEffect` in DailyNotePage, WeeklyNotePage, and BodyPage to satisfy `react-hooks/refs` rule.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npx tsc -b --noEmit` passed.
- Risks:
  - `useSyncExternalStore` for keyboard state is a different reactive model than the previous `useState`+`useEffect`. Behavior is equivalent but debugging differs.
  - Dashboard batch loading changes the order of Tauri IPC calls; if the Rust backend has any serial dependency per invoke, batched calls could contend. Current backend has no such constraint.
- Next:
  - Observe Android stutter behavior after this batch. If further jank remains, profile `DataRootContext`/`PreferencesContext` re-render pressure and consider similar dispatch/state splits.

## 2026-03-26 (Release v0.14.2)
- Date: 2026-03-26
- Goal: Tag and publish v0.14.2 — UI-first performance refactor release.
- Changes:
  - Moved Unreleased changelog entries into `## [0.14.2] - 2026-03-26`.
  - Tagged `v0.14.2` and pushed to origin.
- Validation:
  - CHANGELOG section created and Unreleased cleared.
  - Tag pushed; publish workflow triggered.
- Risks: None beyond normal release CI.
- Next: Verify GitHub Actions artifacts (Windows + macOS) and finalize release notes.

## 2026-03-26 (v1.0.0 Plan Batch B: Distribution + Troubleshooting + Quality Gates)
- Date: 2026-03-26
- Goal: Continue v1.0.0 reliability release plan after recovery-guardrail code batch by completing docs and quality-gate upgrades.
- Changes:
  - Reworked `docs/DISTRIBUTION_POLICY.md` into channel-based policy:
    - defined stable (signed) vs testing (unsigned) channels
    - documented current status (testing channel default)
    - clarified macOS Privacy & Security "Open Anyway" flow + `xattr` fallback
    - documented Android APK sideload expectations and updater artifact policy.
  - Added `docs/TROUBLESHOOTING.md` covering top known issues:
    - macOS quarantine block
    - data root not found / unexpected empty state
    - WebDAV connection failures
    - updater check failures (issue #1 context)
    - Android import/path constraints
    - template apply failures.
  - Upgraded quality/release checklists for v1.0.0 reliability standards:
    - `docs/QA_CHECKLIST.md` now includes soft-delete/undo/purge verification items.
    - `docs/RELEASE_CHECKLIST.md` now includes distribution/troubleshooting docs alignment and release-channel declaration.
  - Added Rust unit tests in `src-tauri/src/lib.rs` for new trash helpers:
    - `trash_and_restore_profile_roundtrip`
    - `purge_old_trash_entries_removes_only_expired_entries`.
  - Updated README project-doc links to include distribution policy and troubleshooting docs.
  - Updated `CHANGELOG.md` `Unreleased` section with reliability/docs updates.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - `cargo test --manifest-path src-tauri/Cargo.toml trash_and_restore_profile_roundtrip` passed.
  - `cargo test --manifest-path src-tauri/Cargo.toml purge_old_trash_entries_removes_only_expired_entries` passed.
- Risks:
  - Stable signed channel policy is documented but still depends on certificate/identity provisioning in CI.
  - Troubleshooting guidance for updater/network errors is pragmatic and may need expansion if new edge cases are reported.
- Next:
  - Continue v1.0.0 plan batch C (README restructure + screenshot refresh).
  - Continue v1.0.0 plan batch D (bilingual GitHub Pages landing page + pages workflow).

## 2026-03-26 (v1.0.0 Plan Batch C+D: README Restructure + Project Homepage)
- Date: 2026-03-26
- Goal: Continue v1.0.0 release plan with user-facing presentation upgrades (README + homepage) and release-facing roadmap alignment.
- Changes:
  - Reworked `README.md` into two clear tiers:
    - top user-facing section (what it is, highlights, screenshots, download/install pointers)
    - bottom developer-facing section (stack, local setup, quality gates, release workflow, docs links).
  - Added README header badges:
    - version
    - license
    - platform support (macOS/Windows/Android)
    - linked project homepage URL.
  - Cleaned README milestone summary to keep only `v1.0+` milestones and link detail backlog to `docs/ROADMAP.md`.
  - Expanded screenshot set to include required `body.png` slot and refreshed screenshot asset notes in `docs/assets/screenshots/README.md`.
  - Added `site/` GitHub Pages bilingual landing page scaffold:
    - static EN/ZH language switch
    - hero + feature cards
    - screenshot gallery
    - release/download CTA links
    - distribution/troubleshooting doc links.
  - Added GitHub Pages deployment workflow: `.github/workflows/pages.yml` (push to `master`/`main` and manual dispatch).
  - Updated `docs/ROADMAP.md` `v1.0.0` section with progress snapshot (done/in-progress).
- Validation:
  - `npm run lint` passed.
  - Manual static checks passed for `README.md`, `site/index.html`, `site/styles.css`, and workflow YAML structure.
- Risks:
  - `docs/assets/screenshots/body.png` is currently a temporary placeholder and should be replaced with real in-app capture before final `v1.0.0` tag.
  - GitHub Pages public URL/activation also depends on repository Pages settings (source = GitHub Actions).
- Next:
  - Replace `body.png` placeholder with real Calm Glass UI body-page capture.
  - Run one end-to-end Pages deploy validation after merge (confirm live site URL + links).
  - Continue v1.0.0 release packaging tasks (`E2`..`E5`) after QA sign-off.

## 2026-03-26 (v1.0.0 Release Packaging Prep + Screenshot Placeholders)
- Date: 2026-03-26
- Goal: Prepare v1.0.0 packaging baseline so release can be tagged quickly after real screenshots are swapped in.
- Changes:
  - Unified release version metadata to `1.0.0`:
    - `package.json`
    - `src-tauri/Cargo.toml`
    - `src-tauri/tauri.conf.json`
    - lockfile root package version updated via cargo check.
  - Prepared changelog release cut:
    - moved current release-ready items into `## [1.0.0] - 2026-03-26`
    - kept `## [Unreleased]` section present and empty.
  - Expanded screenshot placeholder set for release-page-ready coverage:
    - existing: `body.png` placeholder
    - added placeholders: `profiles.png`, `sync.png`, `reports.png`, `onboarding.png`
    - mirrored placeholders to `site/assets/screenshots/`.
  - Updated screenshot guidance and release checklist to explicitly track:
    - core screenshot set (README/homepage)
    - extended screenshot set (release page).
  - Expanded homepage screenshot gallery to include extended slots.
- Validation:
  - `npm run lint` passed.
  - `npm run test -- --run` passed (17 files, 60 tests).
  - `npm run build` passed.
  - `cargo check --manifest-path src-tauri/Cargo.toml` passed.
  - `node --check site/app.js` passed.
- Risks:
  - Extended screenshots are placeholders only; publish-quality release assets still need real captures.
  - Auto-generated release notes script from `v0.14.2..HEAD` currently returns no commits because current changes are not committed yet.
- Next:
  - Replace placeholder screenshots with real Calm Glass UI captures.
  - Commit release-prep changes and regenerate release notes from latest tag to release commit range.
  - Tag `v1.0.0` and run publish workflow after final screenshot swap + QA sign-off.

## 2026-03-26 (v1.0.0 Screenshot Sync Follow-up: Add History, Remove Reports)
- Date: 2026-03-26
- Goal: align screenshot set with release narrative by including Daily/Weekly history and removing reports screenshot slot.
- Changes:
  - Added new placeholder screenshots:
    - `docs/assets/screenshots/daily-history.png`
    - `docs/assets/screenshots/weekly-history.png`
  - Removed reports screenshot placeholder from current set:
    - deleted `docs/assets/screenshots/reports.png`
    - deleted `site/assets/screenshots/reports.png`
  - Synced all PNG screenshots from `docs/assets/screenshots/` to `site/assets/screenshots/`.
  - Updated homepage gallery to include Daily/Weekly history entries and remove Reports entry.
  - Updated screenshot documentation/checklist:
    - core set now includes `daily-history` and `weekly-history`
    - extended set excludes `reports`.
- Validation:
  - Verified docs/site screenshot directories now match PNG inventory.
  - Verified no `reports.png` references remain in site/readme/checklists.
  - `node --check site/app.js` passed.
- Risks:
  - `daily-history` and `weekly-history` are still placeholders and need real captures before final release.
- Next:
  - Replace placeholders with real screenshots after your capture pass.

## 2026-03-26 (Release Hotfix: publish.yml version-sync idempotency)
- Date: 2026-03-26
- Goal: Fix `v1.0.0` publish workflow failure at `Sync app version from tag`.
- Changes:
  - Updated `.github/workflows/publish.yml` in both `publish-tauri` and `publish-android-apk` jobs:
    - keep replacing `src-tauri/Cargo.toml` version from tag when needed
    - if version already matches tag, treat as success (notice log) instead of throwing.
- Validation:
  - Verified workflow script logic now distinguishes:
    - missing Cargo version field (still hard-fail)
    - already-synced version (pass)
    - version mismatch (update + pass).
- Risks:
  - None identified; change is CI-only and idempotency-focused.
- Next:
  - Commit and push workflow hotfix.
  - Re-run `publish` via `workflow_dispatch` with `tag_name=v1.0.0`.

## 2026-03-26 (Pages Workflow Fix: Auto-sync screenshots)
- Date: 2026-03-26
- Goal: avoid manual drift between `docs/assets/screenshots` and `site/assets/screenshots` in GitHub Pages deploys.
- Changes:
  - Updated `.github/workflows/pages.yml`:
    - trigger now includes `docs/assets/screenshots/**`
    - checkout upgraded to `actions/checkout@v6`
    - added deploy-time sync step copying `docs/assets/screenshots/*.png` into `site/assets/screenshots/`.
    - enabled `actions/configure-pages` auto-enable mode (`enablement: true`) for first-run repos without Pages initialized.
- Validation:
  - Verified workflow now fails fast when docs screenshot PNG set is empty.
  - Verified workflow deploy artifact source remains `./site` with synced screenshot assets.
- Risks:
  - None; change is deploy-time asset synchronization only.
- Next:
  - Optionally remove manual screenshot sync habits and treat docs screenshot folder as single source for homepage assets.

## 2026-03-26 (Homepage Refresh: Fancy Marketing Layout + In-page Tutorials)
- Date: 2026-03-26
- Goal: improve project homepage quality for open-source promotion and embed practical tutorials directly in the webpage.
- Changes:
  - Rebuilt `site/` presentation layer:
    - stronger campaign-style hero
    - trust/feature proof cards
    - richer bilingual content hierarchy
    - refreshed visual style with bold gradients, texture, and motion.
  - Added in-page interactive tutorial center (`site/index.html` + `site/app.js`):
    - quick start
    - daily/weekly workflow
    - body tracking
    - WebDAV sync
    - recovery & safety
    - tab-switch interaction with EN/ZH localization.
  - Added tutorial docs hub and companion pages:
    - `docs/TUTORIALS.md`
    - `docs/tutorials/quick-start.md`
    - `docs/tutorials/daily-weekly-flow.md`
    - `docs/tutorials/body-tracking.md`
    - `docs/tutorials/webdav-sync.md`
    - `docs/tutorials/recovery-and-safety.md`
  - Updated README project docs links to include tutorials index.
- Validation:
  - `node --check site/app.js` passed.
  - Verified tutorial tab wiring and key i18n text mapping exist.
  - Verified no stale `reports` screenshot references in site code.
- Risks:
  - Page content density increased; later iteration may need selective collapsing for small screens.
- Next:
  - Capture final real screenshots (replace placeholders) and rerun Pages deploy preview.

## 2026-03-26 (Pages CI Follow-up: Node24 action runtime opt-in)
- Date: 2026-03-26
- Goal: remove Node20 deprecation warning in Pages deployment workflow.
- Changes:
  - Added workflow-level env in `.github/workflows/pages.yml`:
    - `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'`.
- Validation:
  - YAML structure remains valid after update.
- Risks:
  - None expected; aligns with GitHub deprecation guidance.
- Next:
  - Observe next Pages run annotation output to confirm warning removal.

## 2026-03-26 (Homepage Branding Follow-up: Add Logo)
- Date: 2026-03-26
- Goal: strengthen homepage branding by showing the project logo prominently.
- Changes:
  - Added logo asset to site static files:
    - `site/assets/dailytrack-logo.png` (from `src/assets/dailytrack-logo.png`).
  - Updated `site/index.html`:
    - nav brand now includes logo + wordmark
    - hero section includes branded logo block
    - favicon now uses project logo.
  - Updated `site/styles.css` with logo-specific layout and visual treatment.
- Validation:
  - Verified static references resolve to `./assets/dailytrack-logo.png`.
  - `npm run lint` passed.
- Risks:
  - None identified.
- Next:
  - Optional: provide dark/light logo variants for future theme expansion.

## 2026-03-27 (Homepage Visual Upgrade: Hero Mockup + Bento Gallery + Lightbox)
- Date: 2026-03-27
- Goal: address homepage polish feedback with stronger first-screen visual impact, clearer tutorial readability, and richer open-source footer navigation.
- Changes:
  - Reworked `site/index.html` layout:
    - removed stats strip and shifted to a stronger visual hierarchy
    - rebuilt hero as copy + product mockup shell with contextual chips
    - upgraded feature section to icon-led cards
    - upgraded tutorial section to timeline-like numbered steps while preserving in-page tab workflow
    - rebuilt screenshot gallery into bento grid with Daily/Weekly history screenshots emphasized
    - added full-screen lightbox preview for screenshot inspection
    - expanded footer with explore/project/support link groups.
  - Reworked `site/styles.css` visual system:
    - refined gradient background meshes and glass card depth
    - added card/button/image hover lift and shadow transitions
    - added responsive bento behavior and lightbox presentation styles
    - retained mobile-friendly layout behavior for hero, gallery, and footer.
  - Extended `site/app.js`:
    - added new i18n keys for EN/ZH homepage content
    - improved tutorial tabs with ARIA selection states and keyboard arrow navigation
    - added screenshot lightbox open/close behavior (click image, backdrop, or `Esc`)
    - added intersection-based reveal animation trigger for section entrance.
  - Updated release notes docs:
    - appended `CHANGELOG.md` (`Unreleased`) with homepage polish + lightbox additions.
- Validation:
  - `node --check site/app.js` passed.
  - `npm run lint` passed.
- Risks:
  - Hero and gallery are now more animation-rich; low-end devices may still show minor frame drops during first paint.
- Next:
  - Replace hero-first screenshot with final polished capture once the newest UI screenshots are ready.
